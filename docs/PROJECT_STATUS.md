# Projektgedaechtnis — Railway Order Management

> Dieses Dokument wird nach groesseren funktionalen Aenderungen mit dem aktuellen Ist-Stand nachgezogen.

## Letzte Aktualisierung

**2026-06-25** — **A-S4 (Verkehrstage-Picker: Belegung + Umverteilung)**: Jede FAHRPLAN-Karte bekommt eine **Verkehrstage**-Aktion (`OrderPositionRow.addActionChip`), die den neuen `ExpressionVerkehrstageDialog` öffnet. Der `ValidityCalendar` markiert jetzt **von Geschwister-Ausprägungen belegte Tage** (amber/gestrichelt + Tooltip „Belegt: …", `setOccupiedDates`). Wählt man einen belegten Tag, fragt ein **Bestätigungsdialog** und beim Speichern wird der Tag **datums-genau umverteilt**: aus der Geschwister-`validity` entfernt + dort eine **`MODIFICATION`-Version** erfasst (`OrderPositionVersion`, Summary „Verkehrstag(e) … abgegeben an …"). Service: `OrderService.verkehrstageContext(id)` (Kalender-Fenster = **ganzer Zug** = Eltern+Ausprägung+alle Geschwister; + Belegungs-Map) und `setExpressionVerkehrstage(id, days)` (@PreAuthorize ADMIN/DISPATCHER; aktualisiert validity + abgeleitete weekdays; kürzt Geschwister; schreibt Version). i18n `position.action.verkehrstage` + `verkehrstage.*` in 5 Bundles. Verifiziert: Compile/Spotless/ArchUnit (8 grün), **E2E auf frischem Dev-Server: Mo–Fr-Ausprägung gesetzt → Sa–So-Kalender zeigt 140 belegte Tage → alle Freitage gegriffen → Confirm → DB: exprA Freitage 0, exprB +28 Freitage, +1 MODIFICATION-Version mit Datumsliste; 0 pageErrors**, adversarialer 4-Lens-Review + 3× Codex. **Härtung (Codex):** `effectiveDays(pos)` leitet Belegung/Vorauswahl bei noch leerer `validity` aus dem **Wochentag-Template** ab (wird bei Umverteilung materialisiert); Picker füllt die Tage der bearbeiteten Ausprägung vor (kein stilles Verwerfen); Leer-Tag-Speichern blockiert; Versions-Summary auf 500 Zeichen begrenzt (lange Läufe als „N Tage (von–bis)"). Zurückgestellt (dokumentiert): 0-Tage-Geschwister nach Umverteilung, Versionsnummern-Race (= bestehendes `PathProcessEngine`-Muster, atomar). Demo-Daten nach Tests auf V40/V41/V42-Seed zurückgesetzt.

Davor: **2026-06-25** — **A-S3 (Karten-Optik)**: Ausprägungen (und flache Positionen) rendern als **volle-Breite-Karten** (Rahmen/Radius/Margin, **keine Einrückung mehr**); der ZUG bleibt Header darüber (`renderPosition(card, selectable)`, card = nicht-ZUG). Je Bedarf zeigt `ResourcePanel` jetzt **Verkehrstage (Tageszahl)** + **von→nach-Route** als Chips (null-sicher via `ValidityJsonCodec.fromJson`). i18n `resource.days`. Verifiziert: Compile/Spotless/ArchUnit (13), **E2E-Screenshot Zug-Demo (ZUG-Header + Ausprägungs-Karten); 4711 mit Bedarf-Panels rendert (validity=null sicher), 0 pageerrors**.

Davor: **2026-06-25** — **A-S2 (Bedarf-UI: Kalender + von/nach)**: `ResourceDialog` bekommt einen `ValidityCalendar` (begrenzt auf die Positions-Tage; leer = alle Tage) für die **Bedarf-eigenen Verkehrstage** + zwei `OperationalPointComboBox` (lazy, **von/nach — nur bei FAHRPLAN**). Neue `ResourceNeedService.updateVerkehrstageAndRoute(needId, validity, from, to)` persistiert validity + von/nach nach dem `addResource`. `opRepo` durch `ResourcePanel` + `OrderPositionPanel` durchgereicht. i18n `resource.verkehrstage` in 5 Bundles. Verifiziert: Compile/Spotless/ArchUnit (13), **E2E: PERSONNEL-Bedarf mit von=Luzern + Montagen → `validity`+`from_location` in DB, 0 pageerrors**, **adversarialer 4-Lens-Review-Workflow → 0 bestätigte Findings**. (Anzeige der Bedarf-Verkehrstage/Route je Bedarf folgt in A-S3.)

Davor: **2026-06-25** — **A-S1 (Bedarf-Modell: Verkehrstage + von/nach)** auf Branch `feature/expression-model` (vertieftes Ausprägung+Bedarf-Modell, Konzept `docs/plans/concept-expression-and-demand-model.md`): `ResourceNeed` bekommt `validity` (JSONB-Datums-Set ⊆ Positions-Tage, spiegelt `OrderPosition.validity`) + `fromLocation`/`toLocation` (von/nach, nur FAHRPLAN-relevant). Migration `V45` (ALTER `resource_needs` + `_audit`). Verifiziert: Compile/Spotless/ArchUnit (13), V45 Rollback-Tx, **Boot mit Hibernate `validate` bestanden**, **adversarialer 4-Lens-Review-Workflow (Schema/Migration/Semantik/Downstream, je gegenverifiziert) → 0 bestätigte Findings**.

Davor: **2026-06-25** — **UX-Fix Ausprägungen: „+ Ausprägung" ausgegraut statt Spät-Fehler** (auf `feature/pagination`): Der Knopf wird jetzt deaktiviert (mit Tooltip-Begründung), wenn eine flache Position nicht aufgeteilt werden kann — statt erst nach dem Ausfüllen des Dialogs zu scheitern (`OrderPositionActions.canSplit`, spiegelt den Service-Guard). Dabei **Guard korrigiert** (`OrderService.addExpression` **und** UI): es zählen nur **echte Bestellpositionen** (purchases), **nicht** der intrinsische CAPACITY-`ResourceNeed` (Fahrplan-Link jeder FAHRPLAN-Position) — vorher blockierte das fälschlich fast alle Positionen (E2E: 36 statt 2 disabled). Verifiziert: Compile/Spotless, **E2E: in 4711 exakt 2 Knöpfe disabled (die mit echter Bestellung), Zug enabled, 0 pageerrors**. `OrderPositionPanel` 501 Z.

Davor: **2026-06-25** — **Pagination P1 (OP-Combos lazy)** auf Branch `feature/pagination`: Neue wiederverwendbare `OperationalPointComboBox` (server-seitige Paged-Suche über Name/UOPID via der vorhandenen, bisher ungenutzten `findByNameContainingIgnoreCaseOrUopidContainingIgnoreCase(..., Pageable)`; programmatisch gesetzter Wert bleibt off-page erhalten). `ServicePositionDialog` Von/Nach-Combos umgestellt → **kein `opRepo.findAll()` (~19.300 OPs) mehr** beim Dialog-Öffnen; `findOperationalPoint` nutzt jetzt `findFirstByNameIgnoreCase`. Verifiziert: Compile/Spotless/ArchUnit (13), **E2E: Dialog + Lazy-Suche „Luz" → 21 Treffer (erster „Luzern"), 0 pageerrors**. **Noch offen in P1:** der TimetableBuilder (`TimetableBuilderView`/`TimetableRouteStep`/`AddStopForm`) nutzt die OP-Liste als **Arbeits-Map** (UOPID-Auflösung + Routen-Karte), nicht nur als Combo-Auswahl → eigener, größerer Lazy-Umbau (separater Schritt).

Davor: **2026-06-25** — **Cleanup: `OrderPositionPanel`-Split (< 500 Z.)**: Das auf 714 Zeilen gewachsene Panel in drei kohärente Klassen aufgeteilt — `PositionBulkBar` (Auswahl + Sammel-Statuswechsel), `UnassignedTrainsDialog` (Aus-RailOpt-Übernahme), `OrderPositionActions` (Positions-Dialoge/Handler: Service/Löschen/Fahrplan-Builder/Ausprägung/Alteration). Panel jetzt **497 Z.** (Kern: Orchestrierung/Rendering/Filter). Verhaltensgleich verifiziert: Compile/Spotless/ArchUnit (13), **E2E grün** (Hierarchie/Feed/+Ausprägung/Aus-RailOpt-Dialog/Bulk-Bar, 0 pageerrors). Saubere Basis vor dem Pagination-Bau (`docs/plans/build-plan-pagination.md`).

Davor: **2026-06-25** — **K2-S7 (Auto-Bestellung: Knopf + @Scheduled)** — Konzept 2 fertig: Neuer `FristAutoOrderService.runOnce()` feuert `AUTO_BESTELLEN`-Regeln: für jedes Mitglied, dessen Trigger greift (DATUM = Frist erreicht/überfällig, oder STATUS = TTT-Prozessstatus wie Final Offer), wird die Position auto-bestellt (Mock-Effekt: Status `BEANTRAGT`) — genau einmal. **Idempotenz** via `AutoOrderLog` (unique (Position, Regel), `V44`), **Kill-Switch** = `enabled` der Regel. Auslösung per Knopf „Regeln auswerten" in `FristenView` (Inline-Ergebnis-Banner statt Toast — Vaadin-`requestContentUpdate`-Renderfehler im Rebuild umgangen) **und** per `@Scheduled` (langer Delay, `@EnableScheduling`; konfigurierbar `app.frist.auto.*`). Seed `V44`: 1 AUTO_BESTELLEN-Regel. Gruppen-Listen auf 25 gekappt (`fristen.more`). i18n `fristen.evaluate.*`. Verifiziert: Compile/Spotless/ArchUnit (13), V44 Rollback-Tx, **E2E: 38 Bestellungen ausgelöst → 2. Lauf idempotent (0), DB-Beleg (Log + BEANTRAGT), 0 pageerrors**.

Davor: **2026-06-25** — **K2-S6 (Automatische Geschäfte)**: `FristService.automaticBusinesses()` liefert je aktiver Regel ein virtuelles „automatisches Geschäft" mit **dynamischer Mitgliedschaft** (Member-Count + überfällig-/bald-fällig-Aufschlüsselung). `FristenView` zeigt oben eine „Automatische Geschäfte"-Sektion (⚙ Regelname · n Positionen · m überfällig · Aktions-Badge Anzeigen/Auto-Bestellung). i18n `fristen.auto.*`/`fristen.action.*`. Compile/Spotless/ArchUnit grün (visueller Check in der Abschluss-E2E).

Davor: **2026-06-25** — **K2-S5 (Frist-Regeln + Berechnung + Übersicht)** — Start Konzept 2: Neues konfigurierbares Entity `FristRegel` (Mitglieds-Filter NICHT_BESTELLT/ALLE_FAHRPLAN, Anker ABSOLUT/FAHRT/FAHRPLANJAHR_START + Offset, Warn-Schwelle, Trigger DATUM/STATUS, Aktion ANZEIGEN/AUTO_BESTELLEN; `V43` + Audit + 3 Seed-Regeln). `FristService.evaluate()` rechnet je buchbarer FAHRPLAN-Position (ohne ZUG-Container) die effektive Frist + Status (ÜBERFÄLLIG/FÄLLIG_BALD/OK). Neue View `FristenView` (`/fristen`, Nav „Fristen") gruppiert nach Dringlichkeit mit Frist-Badge + „Zum Auftrag". `OrderPositionRepository.findByType` mit Fetch-Join `order` (LazyInit-Fix während E2E gefunden). i18n `fristen.*`/`nav.fristen` in 5 Bundles. Verifiziert: Compile/Spotless/ArchUnit (13), V43 Rollback-Tx, **E2E zeigt Überfällig-Gruppe (80) mit Regel + Frist-Datum, 0 pageerrors**.

Davor: **2026-06-25** — **K1-S4 (Manuelles Anlegen von Ausprägungen + Verkehrstage-Disjunktheit)**: Neues Wochentage-Set je Ausprägung (`OrderPosition.weekdays`, ISO 1-7, `Weekdays`-Util; `V42` + Backfill der V40-Kinder Mo-Fr=1-5 / Sa-So=6,7). `OrderService.addExpression(parentId, ExpressionDraft)` legt eine Ausprägung an, **promotet eine flache Position beim ersten Mal zu `ZUG`**, und erzwingt **Verkehrstage-Disjunktheit** (kein Geschwister teilt einen Wochentag bei überlappenden Daten → `ExpressionConflictException`). UI: „+ Ausprägung"-Button je FAHRPLAN-Zug + `ExpressionDialog` (Bezeichnung/OTN/Wochentage-Checkboxen/Von-Nach/Start-Ende). i18n `expression.*` in 5 Bundles. Verifiziert: Compile/Spotless/ArchUnit (13), V42 Rollback-Tx, **E2E: Button + Dialog gerendert, Disjunktheits-Konflikt abgewiesen, 0 pageerrors**. **Zurückgestellt:** RailOpt-Auto-Spiegel (Multi-SubCalendar-Train → mehrere Kinder) — der RailOpt-Mock hat derzeit keine Multi-Kalender-Trains; dokumentierter Folgeschritt. **Technische Schuld:** `OrderPositionPanel` 705 Z. → eigener Split-Commit folgt.

Davor: **2026-06-25** — **K1-S3 (Versionen + Änderungs-Feed + OTN-Historie)**: Neue UI-Komponente `VersionFeed` zeigt den Änderungsstrang (Basis + Overrides) mit Quelle-Badge (Original/Änderung/Trassen-Änderung/Stornierung) + Gültigkeitsfenster. `OrderPositionPanel` lädt Versionen + OTN-Historie **batched** (`OrderService.findVersionsByPositions`/`findOtnHistoryByPositions` → je 1 Query via `findByOrderPositionIdIn`), zeigt je Ausprägung ihren Feed im Body, je **Zug einen aggregierten** Feed über alle Ausprägungen, eine „war {alte OTN}"-Chip (`OrderPositionRow.setOtnHistory`) und flaggt offene ALTERATIONs im ⚠-Badge. Seed `V41`: OTN-Rename 17000→18000, Ferien-Traverso (Modification, KW 28-31), Baustellen-Alteration (+5 min). Verifiziert: Compile/Spotless/ArchUnit (13), V41 Rollback-Tx, **E2E-Screenshot zeigt den Feed mit Original/Modification/Alteration + OTN-Chip, 0 pageerrors**.

Davor: **2026-06-25** — **K1-S2 (UI Zug → Ausprägungen)**: `OrderPositionPanel.refreshPositions` partitioniert Positionen in Top-Level (ZUG-Identität + Bestand/flach) und Kinder (AUSPRAEGUNG, gruppiert nach `variantOf`); neue Helper-Methode `renderPosition` rendert eine Zeile, Kinder eingerückt (margin-left + Border) unter ihrem Zug. Der ZUG-Container ist nicht bulk-selektierbar (Buchungen hängen an den Ausprägungen); Bestand/flach + Kinder bleiben selektierbar. Bestand rendert unverändert (null = flach = ein Top ohne Kinder, **keine Daten-Migration nötig**). Seed `V40`: Auftrag ZUG-2026-001 mit Zug „S2 18000" + Ausprägungen Mo–Fr 2×Flirt (OTN 18001) / Sa–So 1×Flirt (OTN 18051). Verifiziert: Compile/Spotless/ArchUnit (13), V40 Rollback-Tx, **E2E-Screenshot zeigt Zug + zwei eingerückte Ausprägungen, 0 pageerrors**. Anmerkung: `OrderPositionPanel` 607 Z. (über dem 500-Limit, schon vor S2) — Split als Bereinigung vorgemerkt.

Davor: **2026-06-25** — **K1-S1 (Datenmodell Zug/Ausprägung/Version)** auf Branch `feature/train-identity-variants`: `OrderPosition.variantOf` als Eltern-Link aktiviert, `variantType` → Enum `PositionVariantType` (`ZUG`/`AUSPRAEGUNG`, `null`=Bestand) + Convenience-Collections `children`/`versions` (`@NotAudited`). Neues `@Audited`-Entity `OrderPositionVersion` (versionNumber, `source` `PositionChangeSource` INITIAL/MODIFICATION/ALTERATION/CANCELLATION, validFrom/validTo, changeSummary, payload-JSON) + `PositionOtnHistory` (otn, valid_from/to, source) als OTN-Historie. Repos `OrderPositionVersionRepository`/`PositionOtnHistoryRepository` + `OrderPositionRepository.findByVariantOfId/findByVariantType`. Migration `V39` (2 Tabellen + `_audit`). Verifiziert: Compile/Spotless/ArchUnit (13) grün, **Boot ok = Flyway V39 + Hibernate `validate` gegen das neue Modell bestanden**. Noch ohne UI/Seed (folgt K1-S2).

Davor: **2026-06-25** — **Konzepte: Zug-Identität/Ausprägungen/Versionierung + Frist-Regeln/automatische Geschäfte** (noch nicht gebaut). Zwei Konzept-Docs zur Freigabe in `docs/plans/`: (1) `concept-train-identity-variants-versioning.md` — Drei-Ebenen-Modell **Zug-Identität (stabil, findbar über alte OTN/TRID) → Ausprägung (Kind = OTN × Verkehrstage-Kalender, 1:1 TTT-Bestellung) → Version (zeitlicher Override, Modification/Alteration)**; gegen TTT-Spec verifiziert (ReferenceTrainID = stabiler Anker, OTN = änderbares Label; ein Referenzzug → mehrere disjunkte Paths). Baut auf ungenutztem `OrderPosition.variantOf`/`variantType` + `PmTrainVersion` + Slice-J-`DeviationDetector` auf. (2) `concept-deadline-rules-auto-business.md` — Fälligkeiten ohne Timer-Konstrukt über **regelgetriebene (automatische) Geschäfte**: Frist-Regel = Mitglieds-Filter + Anker (ABSOLUT/FAHRT/TTR_PHASE) + Offset + Trigger (datums-/statusbasiert, z. B. Final Offer) + **Auto-Bestellung vollautomatisch**. User-Entscheide (2026-06-25): Kalender⇒Kind/Zeit+Fahrzeug⇒Version; Kinder primär aus RailOpt, manuell ergänzbar; Auto-Bestellung vollautomatisch; **Konzept jetzt, Bau erst nach Pagination + Customer-/Geschäfts-Views**. Capacity-Supply bleibt komplett out of scope (Inhalte/OTN-Frage offen).

Davor: **2026-06-24** — **Follow-up Slice J (Abweichungs-Erkennung)**: Neuer statischer `DeviationDetector` (ui.component.order) vergleicht eine FAHRPLAN-Position mit ihrem verlinkten RailOpt-Train auf zwei Achsen — (1) Auftrag↔RailOpt: Von/Nach/Start/Ende der Position vs. Train (Kalender + letzte Version), (2) Version↔Original: neueste Train-Version vs. INITIAL (= Trassen-Modifikation). `OrderPositionRow.setDeviations` zeigt ein ⚠-DANGER-Badge mit Tooltip (Detail-Liste); `OrderPositionPanel` ruft den Detector je Position auf. Neue `PathManagerService.findByIdWithVersions` initialisiert die LAZY-Collections (trainVersions + journeyLocations) vor Detach. Seed `V38__seed_deviation_demo.sql`: Auftrag ABW-2026-001 / Position „IC 93500" weicht in Ziel (Zürich HB↔Zug) + Start (15.08.↔01.09.) ab, Train mit v1→v2 Zeitänderung. i18n `deviation.*` + `position.deviation` in 5 Bundles. Compile/Spotless/ArchUnit (13) grün; V38 in Rollback-Tx gegen die DB validiert.

Davor: **2026-06-24** — **Follow-up Slice I (Massenverarbeitung: Bulk-Statuswechsel)**: Im `OrderPositionPanel` (Mutator-Modus) hat jede Position eine Auswahl-Checkbox; eine Sammel-Leiste („n ausgewählt" + Status-`Select` + „Anwenden") setzt den Bearbeitungs-Status über alle ausgewählten Positionen in einer Transaktion via neuer `OrderService.setPositionInternalStatusBulk(Set<UUID>, PositionStatus)`. Auswahl/Leiste werden bei jedem Refresh zurückgesetzt. i18n `bulk.*` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün.

Davor: **2026-06-24** — **Follow-up Slice H (Offene-Positionen-Übersicht)**: Neue View `OffenePositionenView` (`/offene-positionen`, Nav-Eintrag) bündelt „offene" Arbeit (SOB S.21/28): Positionen mit ≥1 offener/nicht-gebuchter Bestellposition (`PurchasePositionRepository.findOpenWithOrder`: `purchaseStatus=OFFEN` ODER `pmProcessState != BOOKED`, je Position gruppiert, Sprung „Zum Auftrag") + nicht zugewiesene Fahrpläne (`findUnassignedTrains`). i18n `offen.*`/`nav.offenePositionen` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün.

Davor: **2026-06-24** — **Follow-up Slice G („Aus RailOpt"-Positionen angereichert)**: `PathManagerService.captureUnassignedTrainAsPosition` erzeugt jetzt zusätzlich ein `TimetableArchive` (aus den Journey-Locations der neuesten Version → `TimetableRowData`-JSON via `writeRows`) + eine CAPACITY-`ResourceNeed` mit `linkedFahrplanId`, sodass eine übernommene FAHRPLAN-Position eine **anzeigbare Tabelle** hat („Fahrplan öffnen") und **TTT-bestellbar** ist (vorher dünner OTN/Route-Stub, dokumentierte Mock-Grenze aus Slice D — jetzt aufgehoben). Bewusst inline in `PathManagerService` gebaut (+`TimetableArchiveRepository`/`ResourceNeedRepository`) statt via `TimetableArchiveService`, um den Bean-Zyklus (`TimetableArchiveService → PathManagerService`) zu vermeiden. Compile/Spotless/ArchUnit (13) + Routing-/Cache-Tests grün.

Davor: **2026-06-24** — **Follow-up Slice F (Jahresbestellung-Demodaten)**: Seed `V37` legt einen Demo-Auftrag `JB-2026-001` „Jahresfahrplan 2026 — Linie S2" mit `created_at` ~12 Monate vor `valid_from` (Lead 364 Tage) + 2 FAHRPLAN-Positionen an → `OrderType.of` ergibt **JAHRESBESTELLUNG**, sodass der Badge (Slice C) in der Demo überhaupt sichtbar wird (zuvor zeigten alle Seed-Aufträge EINZELBESTELLUNG, da `validFrom ≈ createdAt`). V37-SQL in Rollback-Transaktion validiert.

Davor: **2026-06-24** — **Auftrags-Positionen Slice E (R2P-Eingang, Inbox-Mock)**: Eingehende R2P-Bestellungen (ein Dritter sendet einen Fahrplan + Personal-/Fahrzeug-Bedarf) landen in einer Inbox (`R2pInboxEntry`, Status EINGEGANGEN/UEBERNOMMEN) und werden bewusst übernommen. Neue View `R2pInboxView` (`/r2p-inbox`, eigener Nav-Eintrag) mit „Eingang simulieren"-Knopf + Übernehmen-Dialog. `R2pIntakeService`: `simulateIncoming()` (legt Beispiel-Eingang an), `findPending()`, `accept(entryId, fallbackOrderId)` — hängt per **OTN-Match** an eine bestehende FAHRPLAN-Position an, sonst neue LEISTUNG-Position unter gewähltem Auftrag; je Bedarf ein EXTERNAL-`ResourceNeed` (neu `ResourceOrigin.R2P`) + OFFEN-Bestellposition. Ressourcen: Katalog-Referenz **oder** Freitext (`R2pResourceRequest`, als JSON gespeichert). Neu: `ResourceNeedService.addResource`-Überladung mit origin, `OrderPositionRepository.findByOperationalTrainNumber`. Migration `V36` (Inbox-Tabelle, audit-frei = Mock-Staging, dokumentiert). i18n `r2p.*`/`nav.r2pInbox` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün; V36-SQL in Transaktion validiert. **R2P war SOB-Out-of-Scope → frei designt nach User-Vorgaben (OTN-Anhängen sonst neu, Inbox/Übernahme, Katalog+Freitext, Simulieren-Knopf).** Damit ist der 5-Slice-Plan `docs/plans/order-positions-intake-plan.md` vollständig umgesetzt.

Davor: **2026-06-24** — **Auftrags-Positionen Slice D (Nicht-zugewiesen Fahrplan-Intake, Mock)**: Fahrpläne, die im Fahrplanmanager (pm_*) existieren, aber noch keiner Auftragsposition zugeordnet sind (`PmReferenceTrain.sourcePositionId IS NULL`), lassen sich jetzt einem Auftrag als FAHRPLAN-Position zuweisen. Neu: `PmReferenceTrainRepository.findBySourcePositionIdIsNull()`; `PathManagerService.findUnassignedTrains()` + `captureUnassignedTrainAsPosition(trainId, orderId)` (erzeugt FAHRPLAN-`OrderPosition` mit OTN, von/nach aus den Journey-Locations der neuesten Version und Validität aus dem Kalender, setzt `sourcePositionId` zurück); im `OrderPositionPanel` ein Button „Aus RailOpt" + Dialog (mutator-only, listet unzugewiesene Trains, ein Klick übernimmt). Seed `V35` legt 3 unzugewiesene Demo-Trains (+ Version + Origin/Destination-Locations) an. i18n `position.fromPm.*` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün; V35-SQL in Transaktion (ROLLBACK) validiert. PathManagerService nutzt bewusst Order-**Repositories** (kein Bean-Zyklus).

Davor: **2026-06-24** — **Auftrags-Positionen Slice C (Jahres-/Einzel-Badge)**: Neuer abgeleiteter (nicht persistierter) `OrderType` (JAHRESBESTELLUNG/EINZELBESTELLUNG); `OrderType.of(order)` leitet den Typ aus der **Lead-Time Bestelldatum→Betriebstag** ab (SOB §5.3: TTR-Prozess aus Bestelldatum+Betriebstag, Grenze X-2 Monate = 60 Tage) und bildet damit AF1 Jahresfahrplan vs. AF3 Extrazug adhoc ab. Anzeige als `StatusBadge` in `OrderCard` (Übersicht) + `OrderDetailView`-Header; zusätzlich **Auftragstyp-Filter** in der Übersicht (`OrderOverviewView`) → damit ist der „Übersicht"-Teil des Status-Filters (Slice B) erfüllt. i18n `order.type.*` + `filter.field.orderType` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün.

Davor: **2026-06-24** — **Auftrags-Positionen Slice B (Status-Filter im Auftrag)**: `OrderPositionPanel` hat jetzt einen einklappbaren `FilterPanel` (Toggle im Kopf), der die Positionen nach **Bearbeitungs-Status** (`PositionStatus`), **externem TTT-Status** und **Beschaffungs-Status** (`PurchaseStatus`) filtert. Da eine Position mehrere Bestellpositionen mit je eigenem Status trägt, neues generisches **`PredicateSelectFilterField`** im Filter-Framework (Single-Select + BiPredicate-Matcher statt Wert-Gleichheit); `refreshPositions()` filtert vor dem Rendern. i18n `position.filter.*` in allen 5 Bundles. Die Übersichts-Erweiterung (Auftragstyp-Filter) folgt in Slice C zusammen mit dem `OrderTypeResolver`. Compile/Spotless/ArchUnit (13) grün.

Davor: **2026-06-24** — **Auftrags-Positionen Slice A (Kompaktansicht + Status-Rollup)**: Die Positionsliste im Auftragsdetail (`OrderPositionPanel`) hat jetzt im Kopf eine **Kompakt/Voll-Umschaltung** + **„Alle ein-/ausklappen"**; jede Position ist per Chevron einzeln auf-/zuklappbar — Ressourcen-Panel und Geschäfts-Chips sind der einklappbare Body der `OrderPositionRow` (im Kompaktmodus standardmäßig zu, Body wird erst beim Aufklappen gerendert → entlastet Aufträge mit vielen Positionen). Neue kompakte Statuszeile je Position: **kombinierter Bestellpositions-Rollup** (Beschaffungs-Status `PurchaseStatus` + externe TTT-Status `pmProcessState` als Zähler-Badges) via neuem `PurchaseStatusRollup` (+ `ResourceBadges`). i18n `position.view.*` in allen 5 Bundles. Compile/Spotless/ArchUnit (13) grün; visuelle Prüfung der Detailseite braucht SSO-Login → Boot-Smoke am Ende. Erster Slice des Plans `docs/plans/order-positions-intake-plan.md`.

Davor: **2026-06-24** — **Performance-Review Slice 4 (toter Routing-Cache repariert)**: Re-Audit der offenen Slice-Kandidaten (7-Agenten-Workflow gegen aktuellen Code) → alle 7 noch valide; zuerst der `@Cacheable`-Self-Invocation-Fix. Die beiden `@Cacheable`-Methoden (`buildGraph`, `loadOperationalPointsByUopid`) wurden in `TimetableRoutingService` **nur intern** (`this.…`) aufgerufen → der Spring-Cache-Proxy griff nie → die Caches `routingGraph`/`operationalPointsByUopid` waren **still tot**: der ~19k-OP-Routing-Graph wurde bei **jedem** `calculateRoute()` neu gebaut (OP-Loader sogar 2×/Call), und der Startup-Warmup füllte nichts. Fix: beide `@Cacheable`-Methoden in neue Bean `RoutingGraphService` (`domain/timetable/service`) extrahiert und in `TimetableRoutingService` injiziert → Aufrufe überqueren jetzt die Proxy-Grenze und treffen den Cache. Punkt-Lookup **und** Graph werden zusammen als **ein** atomarer Snapshot gecacht (`record RoutingTopology`, Cache `routingTopology`, eine `@Cacheable loadTopology()`) statt als zwei unabhängige Caches — so können Graph und Punkt-Map nie divergieren (Codex-P2: Stale-Graph-Race bei Eviction zwischen Punkt-Lesen und Graph-Bau ausgeschlossen). Cache-Invalidierung (Codex-P2): nach erfolgreichem RINF-Import publisht `RinfImportService` ein `RinfDataImportedEvent`; `RoutingGraphService` lauscht (`@EventListener` + `@CacheEvict allEntries`) und leert `routingTopology` → frisch importierte Topologie ist sofort routbar (kein Warten auf die 5-Min-Expiry). Event-basiert gelöst, um einen Kontext-Zyklus infrastructure↔timetable zu vermeiden. Neuer `RoutingGraphServiceCacheTest` (`@SpringJUnitConfig` + `@EnableCaching` + Mock-Repos): zwei `calculateRoute`-Aufrufe lesen Section- und OP-Liste je nur **1×**; ein zweiter Test beweist, dass das Import-Event den Cache evictet. Drei Codex-Review-Runden (Self-Invocation → redundanter Load → Cache-Divergenz) durchlaufen, alle Findings behoben; ein vierter, generischer evict-during-compute-Race (ms-Fenster, selbstheilend ≤5-Min-TTL) bewusst akzeptiert und im Code dokumentiert (Generation-/Lock-Guard wäre Over-Engineering für seltene Admin-Importe). Compile/Spotless/ArchUnit (14 Tests, inkl. Naming-Regel `services_should_be_suffixed` → Bean heißt bewusst `…Service`, nicht `…Cache`)/Routing-Unit-Test/Cache-Regressionstest grün. **Offen (Slice 5+):** OP-Listen lazy in Combos (~19.300 OPs/Combo, Paged-Query existiert ungenutzt), `BusinessLinksTree` (kein Full-Scan pro Checkbox-Toggle), Status-Pill-Dedup (`BusinessReadView`/`BusinessCard`), Timetable-UI-Splits (`TimetableRowEditorPanel` 961/`TimetableBuilderView` 765/`TimetableTableStep` 915), `BusinessDocument`-Blob nur im Detail-Render (Grid unbetroffen → niedrige Prio), Constraint→Zeitfeld-Mapping ⚠ NICHT naiv auf `TimetableRowData` zentralisieren (WINDOW-Edge variiert absichtlich → parametrisierter Resolver + Tests zuerst).

Davor: **2026-06-23** — **Performance-Review Slice 3**: `BusinessReadView` lädt das Aggregat nicht mehr 4–5× (Konstruktor `getById` + je Karte erneut `getLinkedOrderPositions`/`getLinkedPurchasePositions`/`getDocuments`, die intern jeweils wieder `findById` machten), sondern **einmal** über neue `BusinessService.loadReadModel(id)` → Record `BusinessReadModel` (1 `findById` + Init der drei Collections in einer Transaktion); die drei Karten nutzen die geladenen Listen. Build/Spotless grün, Browser-Smoke Geschäfts-Detail grün (0 Errors). **Offen (Slice 4+):** `BusinessLinksTree` (kein Full-Scan pro Checkbox), `BusinessDocument`-Blob LAZY/Projektion, `@Cacheable`-Self-Invocation, OP-Listen lazy in Combos, Order-Detail-Batch-DTO; Vereinfachung (L): Timetable-UI-Splits + Constraint→Zeitfeld-Mapping zentralisieren.

Davor: **2026-06-23** — **Performance-Review Slice 2**: (1) `OrderService.dashboardStats` macht statt `findAll()` + Java-Schleife jetzt **2 Aggregat-Queries** (`countByProcessStatusGrouped` GROUP BY + `countCriticalDeadlines`) — gegen DB-Ground-Truth verifiziert (Active 2, Order 1, Product/Service 1). (2) Business-n:m-Collections `List → Set` (`LinkedHashSet`): Link/Unlink = gezieltes INSERT/DELETE einer Join-Zeile statt vollem Rewrite + automatisches Dedup. (3) Neue `BusinessService.setOrderPositionLinks(opId, Set<bizIds>)` — **ein** Transaktions-Reconcile statt der Schleife mit einem Service-Call pro Link/Unlink; `BusinessLinkField.applyTo` nutzt sie nun (Hilfsmethode `tryRun` entfernt). Build/Spotless/ArchUnit grün, Boot ok (neue JPQL + Set-Mapping validiert), Browser-Smoke Dashboard/Auftrag/Geschäft grün (0 Errors). **Offen (Slice 3+):** `BusinessReadView` (Aggregat 1× laden statt 4–5×), `BusinessLinksTree` (kein Full-Scan pro Checkbox), `BusinessDocument`-Blob LAZY/Projektion, `@Cacheable`-Self-Invocation, OP-Listen lazy in Combos, Order-Detail-Batch-DTO, Timetable-UI-Splits + Constraint-Mapping zentralisieren.

Davor: **2026-06-23** — **Performance-Review Slice 1 (N+1-Quick-Wins)**: Aus dem gemeinsamen Codex+Fleet-Review die billigsten, wirkungsstärksten Performance-Fixes. (1) Globales `hibernate.default_batch_fetch_size: 50` in `application.yaml` → kollabiert Lazy-Collection-N+1 app-weit. (2) **Geschäfts-Chip-Queries gebatcht** (der in dieser Session eingebaute N+1): neue `BusinessRepository.findBusinessesByOrderPositionIds/…PurchasePositionIds` (IN-Query) + `BusinessService.findByLinkedOrderPositions/…PurchasePositions` → `Map<UUID,List<Business>>`; `OrderPositionPanel` und `ResourcePanel` holen die Verknüpfungen jetzt einmal pro Render statt pro Zeile. (3) Toten, latent kaputten Repo-Code gelöscht (`PmReferenceTrainRepository.findByYearWithDetails`/`…WithVersions`, ungenutzt, `MultipleBagFetchException`-Risiko). (4) 24-Zeilen-Nested-Stream in `OrderPositionRow` → 2-Zeiler. Build/Spotless/ArchUnit grün, Boot ok (neue JPQL validiert), Browser-Smoke der Auftrags-Detailansicht grün (0 Errors). **Offen (nächste Slices):** Order-Detail-Batch-Service, BusinessLinksTree/BusinessReadView Reload-Reduktion, BusinessDocument-Blob LAZY, `BusinessLinkField` Batch-Reconcile + n:m `List→Set`, `dashboardStats` GROUP BY, `@Cacheable`-Self-Invocation, OP-Listen lazy in Combos, Timetable-UI-Splits + Constraint-Mapping zentralisieren.

Davor: **2026-06-23** — **Auftragspositions-UI + Geschäfts-Verknüpfung (n:m) + Audit**: Großer Demo-Feinschliff an den Auftragspositionen/Bestellpositionen und der Geschäfts-Verknüpfung. **(A) Bestellpositionen/Ressourcen:** R²P-Mock für externe Nicht-Kapazitäts-Bedarfe (Lokführer) — „Bestellen (R²P)"-Button + „R²P"-Badge wie TTT (`PurchaseOrderService.triggerR2pOrder`); Bestellpositions-**Beschreibung** wird jetzt angezeigt (war write-only); „Kapazität"-Dopplung entfernt (zeigt stattdessen die Route); Aktionsreihe der Position als **beschriftete Chips** (`op-action`-CSS, RailOpt-Button entfernt); Kopf-Badges (Typ/Bearbeitungs-Status/TTT-Status) aus der Zeile entfernt; **Orphan-Removal-Härtung** in `createPurchasePosition`/`addResource` (verschwindende Bestellpositionen behoben); **LazyInit-Fix** in `createPurchaseRow` (geladene `ResourceNeed` statt Lazy-Proxy). Hilfsklassen ausgelagert: `PurchaseOrderButtons`, `ResourceBadges`. **(B) Geschäfts-Verknüpfung (Business ↔ OrderPosition / PurchasePosition, n:m):** klickbare **Geschäfts-Chips** (`BusinessChips`) an Auftrags- **und** Bestellpositionen → Sprung ins Geschäft; **Verknüpfen beim Anlegen** der Position (`BusinessLinkField` = Multiselect + „Neues Geschäft" inline) in beiden Editoren (ServicePositionDialog + TimetableBuilderView), Delta-Link nach Speichern; **vereinheitlichte Positions-Detailansicht** — „Ansicht"/View (alle Typen) **und** Geschäfts-Links öffnen denselben Screen (`OrderPositionDetailView`: Felder + Bearbeitungs-Status + „Fahrplan öffnen" für FAHRPLAN + verknüpfte Geschäfte). **(C) Audit (Envers):** `@Audited` auf beide `@ManyToMany`-Collections in `Business` → Link/Unlink wird jetzt protokolliert (Audit-Join-Tabellen V28/V29 füllen sich); `AuditService.getPurchasePositionHistory` ergänzt; `compareSnapshots` überspringt die Collections (kein Lazy-Crash). i18n `position.businesses*`, `business.create.inline`, `common.create`, `position.openTimetable`, `purchase.r2p.order`, `position.action.*` (5 Sprachen). Verifiziert: Boot ok (Schema-Validierung mit @Audited grün), **automatisierte Browser-Klicktests über alle 9 Hauptpfade + neue Flows grün (0 Page-Errors, 0 Server-Exceptions)**, Codex-Review sauber, Spotless + ArchUnit (8/5/1), alle Dateien < 500 Zeilen. **Offen/zurückgestellt:** (#3) Auftragsposition direkt beim Anlegen eines **Geschäfts** erzeugen; aktive „Geschäft verknüpfen"-Aktion **an** einer Bestellposition (Anzeige via Chips ist da, Verknüpfen läuft heute über die Geschäfte-Seite).

Davor: **2026-06-23** — **A-Nacharbeiten: Bearbeitungs-Status sichtbar + Rollen-Gating Geschäfte + vollständige Sperre**: (1) **Bearbeitungs-Status in Übersicht**: `OrderCard` zeigt den internen Status als Badge; `OrderOverviewView` hat einen zusätzlichen Filter „Bearbeitungs-Status" (neben dem Prozess-Status-Filter). (2) **Geschäfte rollen-gegated analog zu Aufträgen**: `BusinessOverviewView` (New-Button + Shortcut + `beforeEnter` bounced `/businesses/new` und zeigt bei `/edit` für Nicht-Mutatoren die Read-Ansicht), `BusinessReadView` (Edit/Delete nur für `ADMIN`/`DISPATCHER`), `BusinessCard` (Status-Select wird für Nicht-Mutatoren zur statischen Pille). (3) **Effekt-2-Sperre ist rollenbasiert (SOB §5.7-treu)**: §5.7 sperrt den **Auftraggeber** („der Inhalt seitens Auftraggeber kann nicht mehr angepasst werden"), während **die Planung** während «in Bearbeitung» weiterarbeitet. Daher ist die Sperre = Rollen-Gating: alle Mutations-Controls (`OrderDetailView` Edit/Delete, `OrderPositionPanel` Add-Position, `OrderPositionRow` Edit/Delete + Alteration-Accept/Reject, `ResourcePanel` Add-Resource/Add-Purchase/Trigger-All/Sync/TTT-Order) sind über ein gemeinsames `editable = canMutate` (ADMIN/DISPATCHER) gegated; der Fahrplan-Builder ist ohnehin `@RolesAllowed`. `IN_BEARBEITUNG` erscheint als „🔒 gesperrt"-Hinweis nur beim Auftraggeber (Nicht-Mutator). Damit lösen sich die wiederholten Codex-Funde (Planer-Editierwege via ResourcePanel/Alteration/Fahrplan) auf, weil Planer-Bearbeitung während «in Bearbeitung» korrekt ist. (4) **Refactor**: interne Status-UI aus `OrderDetailView` in neue Komponente `OrderInternalStatusBar` ausgelagert → `OrderDetailView` wieder < 500 Zeilen (war durch A1 auf 562 gewachsen). Build grün, Spotless + ArchUnit/JavaDoc/Naming bestanden; alle berührten Dateien < 500 Zeilen; adversariales Review (Major Resource-Sperre) behoben. **Bekannt/offen (MINOR):** Zuweisungs-Combo (`AssigneeComboBox`) ist auf Karten weiterhin für alle sichtbar (Zuweisung „immer möglich"; Fehlversuche werden geschluckt) — bewusst so belassen.

Davor: **2026-06-23** — **Status-Effekte + Kostenträger + Bearbeitungs-Status (SOB Phase A1)**: Umsetzung der vier Status-Auswirkungen aus §5.7 + Rollen-Gating (vormals MVP-Slice 5). (1) **Kostenträger/PSP-Element**: neues einfaches Feld `Order.costCenter` (Migration `V34`, inkl. `orders_audit`), im `OrderFormPanel` erfassbar. (2) **Auftrag-Bearbeitungs-Status**: das bisher tote Freitext-Feld `Order.internalStatus` ist jetzt der getypte Enum `PositionStatus` (deutsch, 7 Werte). In der `OrderDetailView` als Select (für Mutatoren) bzw. read-only Badge (sonst) unter dem Prozess-Stepper. (3) **Effekt 1 — Freigabe-Pflichtfeld**: `OrderService.setInternalStatus` wirft `CostCenterRequiredException`, wenn auf `FREIGEGEBEN` gesetzt wird, ohne dass ein Kostenträger erfasst ist (Service-Guard + UI-Hinweis). (4) **Effekt 2 — Sperre bei «in Bearbeitung»**: Edit-Button wird ausgeblendet, solange `internalStatus`=`IN_BEARBEITUNG` (reine UI-Sperre wie gewünscht; der Status selbst bleibt änderbar → Rückgabe via «überarbeiten»). (5) **Effekt 3 — manuelles Zuweisen**: bereits durch die Always-on-`AssigneeComboBox` aus A3 abgedeckt («immer möglich»). (6) **Effekt 4 — «shadow»/Modification**: RailOpt-seitig, out of scope (dokumentiert). (7) **Rollen-Gating UI**: neue `CurrentUserHelper.hasAnyRole(...)`; New/Edit/Delete/Status-Stepper nur für `ADMIN`/`DISPATCHER` sichtbar, `/orders/new` bounced Nicht-Mutatoren zurück (Service-Layer-Guards bestehen bereits flächendeckend). (8) **Datenbereinigung:** `V34` setzt alt-Freitextwerte in `internal_status` (≠ Enum-Name) auf `NULL` (orders + audit), damit der Enum-Mapping-Wechsel keine Bestandszeilen beim Laden crasht. i18n `order.costCenter*`/`order.internalStatus*`/`order.locked.*` (5 Sprachen, doppelter Alt-Key `order.internalStatus` bereinigt). Build grün, Spotless + ArchUnit/JavaDoc/Naming bestanden; adversariales Review: 1 Blocker (Legacy-Freitext-Crash) **behoben**. **Offen:** Bearbeitungs-Status optional auch in der Auftragskachel/Filter; Geschäfte-Seite analog rollen-gaten.

Davor: **2026-06-23** — **Zuweisung + wiederverwendbarer Filterbereich (SOB Phase A3)**: (1) **Auftrag-Zuweisung** analog zu Geschäften: `Order.assignmentType`/`assignmentName` (Migration `V33`, inkl. `orders_audit`), `OrderService.setAssignee` mit Rollen-Guard `hasAnyRole('ADMIN','DISPATCHER')` und No-Op bei Unverändert (schont Envers-Audit), `OrderCard` mit derselben `AssigneeComboBox` (Keycloak-User) wie `BusinessCard`. `OrderOverviewView`-Callback kapselt den Aufruf wie die Geschäfte-Seite in `try/catch` → abgelehnte Zuweisung bricht die UI nicht ab. Beide Seiten verhalten sich jetzt identisch („möglichst gleich"). (2) **Wiederverwendbarer Filterbereich** im `MasterDetailLayout`, identisch für Aufträge **und** Geschäfte: kompakte Leiste + ausklappbares Panel (Toggle „⚑ Filter" mit Aktiv-Zähler-Badge) + Chip-Reihe mit Einzel-Entfernen und „Alle löschen". Neue, domänenfreie, generische Bausteine unter `ui/component/masterdetail/filter/`: `FilterField`-Interface + `FilterChip` + `SelectFilterField`/`DateRangeFilterField`/`TextFilterField`/`ToggleFilterField` + `FilterPanel`. Kriterien je View: Status, Gültigkeits-Zeitraum (Overlap), Tags, „Mir zugewiesen" (USER-Zuweisung == aktueller Keycloak-User). (3) **Refactor:** `MasterDetailLayout.Spec` + `TriFunction` in eigene Top-Level-Dateien (`MasterDetailSpec`, `TriFunction`) ausgelagert → `MasterDetailLayout` von 498 auf 347 Zeilen (Datei-Größen-Regel). (4) i18n `filter.*` (9 Keys) in 5 Sprachen. Build grün, Spotless + ArchUnit/JavaDoc/Naming bestanden; adversariales Review ohne Blocker (Username-Identität, Date-Overlap, Panel-Lifecycle, Reset-Schleifen, Vaadin-API explizit geprüft). **Offen:** Kunden-View bekommt den Filter später; danach A1 (Status-Effekte + Kostenträger + Rollen-Guards) als eigener Commit.

Davor: **2026-06-22** — **Alteration auftragsseitig (E2E-MVP Schritt 4)**: RailOpt-initiierte Trassen-Änderungen (Alteration) sind jetzt am Auftrag sichtbar und bedienbar — vorher nur im Fahrplanmanager. (1) `OrderPositionRow`: ALTERATION_ANNOUNCED/ALTERATION_OFFERED → amber Badge; bei ALTERATION_OFFERED erscheinen **„Änderung annehmen / ablehnen"**-Buttons direkt am Auftrag. (2) `PurchaseOrderService.respondToAlteration(orderPositionId, accept)` führt ACCEPT_/REJECT_ALTERATION auf dem Referenzzug aus und re-synct den Bestellstatus; `OrderPositionPanel` verdrahtet die Buttons + zeigt Bestätigung/Fehler, `refreshPositions()` im `finally`. (3) **Bugfix Status-Mapping:** `mapProcessStateToPurchaseStatus` setzte ALTERATION-/MODIFICATION-/REVISION-States auf `null` (Status ging verloren) — jetzt ALTERATION_*/MODIFICATION_REQUESTED → BESTAETIGT, REVISION_REQUESTED → BESTELLT, `null` nur noch für NEW. (4) i18n `order.alteration.*` in 5 Sprachen, `common.errorGeneric` ins Basis-Bundle ergänzt. Präsentator kündigt die Alteration weiterhin über die vorhandenen Aktions-Buttons im ProcessSimulationPanel an (IM_ANNOUNCE_ALTERATION → IM_ALTERATION_OFFER). Build grün, ArchUnit/JavaDoc/Naming/PathProcessEngine bestanden; adversariales Review ohne Blocker.

Davor: **2026-06-22** — **Auto-Übertragung statt Sende-Button (E2E-MVP Schritt 3)**: Auf Wunsch klarer gemacht, dass „Anlegen" und „Übertragen an RailOpt" *ein* Akt sind. (1) Der separate „An RailOpt senden"-Button (war ein No-Op, da der Zug schon beim Speichern via `syncFromTimetable` angelegt wird) ist **entfernt**; der RailOpt-Button an der Position ist jetzt **reiner „In RailOpt anzeigen"-Deep-Link** (nur bei übertragener FAHRPLAN-Position). (2) **Hinweis im Fahrplanbuilder**: Tooltip am Speichern-Button (`timetable.save.railoptHint`) + erweiterte Erfolgsmeldung „… automatisch an RailOpt übertragen" (5 Sprachen). (3) Aufräumung: `OrderPositionPanel.sendToPathManager` + die nun ungenutzte `TimetableArchiveService`-Abhängigkeit aus `OrderPositionPanel`/`OrderDetailView` entfernt, verwaister i18n-Key `position.sendToPm` gelöscht. Build grün, ArchUnit/JavaDoc/Naming 14/14; adversariales Review ohne Blocker (Konstruktor-Verkettung exakt geprüft). **Konzept bestätigt:** Auto-Senden bleibt, Button raus, Hinweis beim Speichern.

Davor: **2026-06-22** — **Deep-Link Auftrag ↔ RailOpt (E2E-MVP Schritt 2)**: Behebt den schlimmsten Navigations-Bruch. (1) **Auftrag → RailOpt:** der RailOpt-Button einer übertragenen FAHRPLAN-Position navigiert jetzt mit `?train=<id>` (`QueryParameters`) statt zum globalen Baum; `PathManagerView` implementiert `BeforeEnterObserver`, liest den Param und wählt in `onAttach` den Zug aus (`selectTrain`: expand Jahr-Knoten + select Zug-Knoten + Detail rendern). (2) Damit Selektion/Expand mit frisch konstruierten Knoten matcht, überschreibt der TreeGrid-DataProvider `getId()` → `TreeNode.id()` (UUID) (vorher Identitäts-basiert über Entities ohne equals). (3) **RailOpt → Auftrag:** Back-Link „Zum Auftrag" im Train-Detail über `PmReferenceTrain.sourcePositionId` → neue Projektion `OrderPositionRepository.findOrderIdById(...)`. (4) Tree-„State"-Column nutzt jetzt die lesbaren `pm.state.*`-Labels (vorher roher Enum-Name). i18n `pm.backToOrder` in 5 Sprachen. Build grün, ArchUnit/JavaDoc/Naming bestanden; adversariales Review: gemeldeter LazyInit-„Blocker" als **False Positive** verifiziert (kein Bytecode-Enhancement → `@ManyToOne(LAZY)` faktisch eager; identisches Muster wie der bestehende `PathProcessEngine`). Detail rendert zuverlässig; Tree-Hervorhebung best-effort.

Davor: **2026-06-22** — **Planungsstatus aus RailOpt (E2E-MVP Schritt 1)**: Erster Baustein des durchgängigen Workflows — der Planungsstatus aus RailOpts Umlaufplanung fließt zurück und wird am Auftrag sichtbar. (1) Neues Enum `PmPlanningStatus {UNPLANNED, PLANNED, ON_SHELF, ON_PHYSICAL_RESOURCE}` + Feld `planningStatus` auf `PmReferenceTrain` (orthogonal zu `processState`), Migration **V32** (`pm_reference_trains` + `_audit`, Default `UNPLANNED`). (2) `PathManagerService.updatePlanningStatus(...)` (rollen-geschützt). (3) RailOpt-seitiger Setter: ComboBox „Planung" in `TrainHeaderPanel`, speichert sofort + refresht via `onSaved`. (4) Auftragsseite: 2. Badge **„Planung · \<Status\>"** in `OrderPositionRow` neben dem RailOpt-Status (Farben: geplant=info, Ablage=warning, auf Fahrzeug=success), aufgelöst über `OrderPositionPanel.resolveTrain`. (5) Lesbare `pm.planning.*`-Labels in allen 5 Sprachen. Build grün, ArchUnit/JavaDoc/Naming/PathProcessEngine-Tests bestanden; adversariales Review ohne Blocker. **Demo-Beat:** Planer setzt im Fahrplanmanager „Geplant"→„Auf Fahrzeug", der Auftrag zeigt sofort das 2. Badge. — Entscheidungen für den E2E-Ausbau: Kosten erstmal raus (KPIs aus Status+Pünktlichkeit), MVP-Kern zuerst, Rollentrennung zeigen.

Davor: **2026-06-22** — **Sende-Flow: RailOpt-Übertragung + Status im Auftrag sichtbar**: Damit im Auftrag sofort erkennbar ist, dass ein Fahrplan an RailOpt übertragen wurde und welchen „Stand" er dort hat. (1) **`OrderPositionRow`** zeigt bei übertragenen FAHRPLAN-Positionen (pmReferenceTrainId gesetzt) ein farbcodiertes Status-Badge **„RailOpt · \<Status\>"** im Kopf der Position (gebucht=grün, storniert/zurückgezogen/keine-Alternative=rot, neu/ersetzt=neutral, sonst info). (2) **`OrderPositionPanel`** löst den aktuellen RailOpt-Prozessstatus je Position via `PathManagerService.findById(...).getProcessState()` auf (guarded — `null` bei geleertem Mock). (3) **15 lesbare `pm.state.*`-Labels** (NEW…SUPERSEDED) in allen 5 Sprachen; vorher wurde der rohe Enum-Name angezeigt. (4) **`ProcessSimulationPanel`** nutzt jetzt diese Labels (`pm.state.<NAME>`) statt `state.name()`, sodass auch die RailOpt-View kundentaugliche Status-Texte zeigt. Build grün (`mvn compile`), 14/14 Architektur-/JavaDoc-/Naming-Tests.

Davor: **2026-06-22** — **Begriffe auf RailOpt umgestellt (UI + Docs)**: Der externe Gegenpart des in-App-„Fahrplanmanagers" ist **RailOpt** (QNamic-Planungssystem), kein generischer IM. Klargestellt, damit der User merkt, dass er an RailOpt sendet. (1) **UI/i18n** in allen 5 Bundles: `position.sendToPm` → „An RailOpt senden", `position.sentToPm` → „An RailOpt gesendet", `position.viewInPm`/`purchase.linkToPm` → „In RailOpt anzeigen", `pm.title` → „RailOpt · Fahrplanmanagement", `pm.reset.title/done/text` auf „RailOpt-Mock", neuer `pm.view.badge` „RailOpt (Demo)". `PathManagerView`-Header-Badge (vorher hartcodiert „PATH MANAGER (MOCK)") nutzt jetzt den i18n-Key; `@PageTitle` → „RailOpt". (2) **Docs**: `pathmanager/package-info` (Stand-in für RailOpt), ADR-011 Nachtrag „External system is RailOpt (QNamic)" mit End-to-End-Flow, GLOSSARY (Fahrplanmanager-Eintrag + neuer RailOpt-Eintrag), ARCHITECTURE (Path-Manager-Sektion). (3) Workflow/Rückfluss-Modell (Business legt Fahrplan an → an RailOpt; Planer plant Umläufe in RailOpt + legt Bestellposition an; TTT-Bestellung aus dem Auftragsmanagement; danach Monitoring + KPIs; Rückfluss: TTT-Status, IST-Fahrdaten, von/nach-Zug-/Activity-Folge, Planungsstatus) im Projektgedächtnis dokumentiert.

Davor: **2026-06-22** — **Demo-Vorbereitung: interaktiver Auftragsstatus, Navigations-Gruppierung der RailOpt-Platzhalter, Dashboard-KPIs, Copy-Bugfixes**: Vorbereitung der Kundenvorfuehrung mit Fokus auf den sichtbaren Auftrags-Workflow. (1) **Interaktiver ProcessStatus**: Neue Komponente `OrderStatusStepper` (`ui/component/order/`) rendert die fuenf Phasen (Auftrag→Planung→Produkt/Leistung→Produktion→Abrechnung) als klickbare Leiste; `OrderDetailView.changeStatus()` setzt den Status (rollen-geschuetzt ueber `OrderService.save`, Envers-auditiert) und stellt bei abgelehntem/fehlgeschlagenem Save den vorherigen In-Memory-Status wieder her, damit ein spaeterer Edit-Save keinen ungewollten Statuswechsel mitpersistiert. Tastatur-Aktivierung (Enter/Space mit `preventDefault`), ARIA-`aria-current`. Status war zuvor nur Anzeige-Pille. (2) **Navigation entruempelt**: Fahrplanmanager und Rollmaterial sind *Test-Platzhalter, die spaeter aus dem externen System RailOpt gespeist werden* (Projektentscheidung 2026-06-22); beide liegen jetzt in einer eingeklappten SideNav-Gruppe "RailOpt (Demo)" (`nav.external`, Kolben-Icon) statt prominent im Hauptmenue. (3) **Dashboard mit echten Zahlen**: `OrderService.dashboardStats()` (record `DashboardStats`, ein einziger Order-Scan) liefert aktive/in-Planung/in-Produktion/kritische-Fristen (≤30 Tage Restgueltigkeit, ausser Schlussphase); `DashboardView` zeigt die KPIs plus eine Balken-Aufschluesselung der Auftraege je Phase statt des Platzhalters. (4) **Bugfixes (Codex-aehnliches Review)**: `PathProcessEngine.copyJourneyLocations` kopiert beim Versions-Klon (IM-Draft/Final/Alteration-Offer) jetzt auch `tttPayload` und `associatedTrainOtn` — vorher gingen das verlustarme TTT-Payload und die Von/Fuer-OTN bei jedem IM-Angebot verloren. `PathManagerController.createInitialVersionFromDto` (REST-Submit `POST /api/v1/pathmanager/trains`) persistiert jetzt `activityCodes` (als JSON-Array `["..."]`) und `associatedTrainOtn`, die zuvor still verworfen wurden. (5) **ArchUnit**: `ArchitectureTest.services_should_be_suffixed` akzeptiert nun zusaetzlich den Suffix `Builder` (fuer den bewusst eingefuehrten `TttDraftBuilder`-Stereotyp im Service-Package). (6) **i18n**: neue Keys `nav.external`, `dashboard.breakdown.title`, `order.phase.workflow/setTo/changed/denied` in allen fuenf Bundles (de/en/it/fr + Basis). Build gruen (`mvn compile`), 14/14 Architektur-/JavaDoc-/Naming-Tests bestanden.

Davor: **2026-05-22** — **Fahrplan-Persistenz an TTT-Halbfenster angepasst + TTT-Draft-Datenstrom und UI erweitert**: `TimetableArchiveService` validiert `TimeConstraintMode.AFTER` und `BEFORE` jetzt als single-bound TTT-Timings (ELD/ELA bzw. LLD/LLA) statt sie wie vollstaendige `WINDOW`-Paare zu behandeln. Damit laesst sich ein Fahrplan speichern, wenn am Origin nur "Abfahrt fruehestens 10:00" gesetzt ist. Die Positions-Start-/Endzeit verwendet nun auch AFTER/BEFORE/COMMERCIAL-Anker statt auf Schaetzzeiten zurueckzufallen. Neuer `TttDraftBuilder` baut aus Auftrags-Fahrplanzeilen ein TTT-nahes Draft-Modell mit `Timing 0..*` je JourneyLocation; der Fahrplanmanager-Mock speichert dieses verlustarme Payload in `pm_journey_locations.ttt_payload`, waehrend die alten Arrival-/Departure-Einzelfelder nur Anzeige-/Kompatibilitaetsprojektion bleiben. `PathManagerService` synchronisiert vorhandene Timing-Anker, Qualifier und Offsets fuer EXACT/WINDOW/AFTER/BEFORE/COMMERCIAL konsistenter ins Path-Manager-Modell. Der Fahrplanbuilder kann im Zeileneditor nun TTT-Ortstyp, explizite TTT-Relevanz fuer Nicht-Halte, mehrere TrainActivityType-Codes, LocationSubsidiaryCode und einfache NetworkSpecificParameter-Zeilen erfassen; diese Werte werden in `TimetableRowData` gespeichert und vom Draft-Builder in das Payload uebernommen. `TimetableArchiveServiceTest` deckt den Leipzig-Hbf-nach-Karlsruhe-Hbf-Fall mit `departureMode=AFTER` und fehlendem `latest` ab; `TttDraftBuilderTest` sichert WINDOW-Paare als `ELD+LLD`, Half-Windows als single `ELD` und die neuen UI-zu-Draft-Felder.

Davor: **2026-05-22** — **Fahrzeugumlauf entfernt**: Die Fahrzeugumlaufplanung wurde aus der aktiven Anwendung herausgenommen, weil der Von/Fuer-Ansatz fuer Bestellungen/Auftragspositionen zu aufwendig war. Entfernt wurden `domain/vehicleplanning/`, die Route `/vehicleplanning`, die zugehoerigen UI-Komponenten, Navigation, `vp.*`-i18n-Keys, Fahrzeugumlauf-Wiki/Konzeptdocs und die Vehicle-Planning-E2E-Szenarien. Rollmaterial-Stammdaten (`rollingstock`) bleiben erhalten. Die historischen `vp_*`-Flyway-Migrationen bleiben im Repository, damit bestehende Datenbanken weiterhin Flyway-validieren.

Davor: **2026-05-05** — **Timetable-Editor-Refactor (umfangreiche Logik-Umstellung)**: Editor und Service auf das vollständige TTT-Regelwerk gehoben.
(1) **`TimeConstraintMode`** auf **6 Werte** erweitert: NONE, EXACT, WINDOW, **AFTER (≥)**, **BEFORE (≤)**, COMMERCIAL. AFTER/BEFORE sind TTT-Halbfenster (single-bound, ELA-only oder LLA-only — TTT-Spec Anlage 1 §3.6 erlaubt `Timing 0..*`).
(2) **`userEntered*`-Flags** pro Zeitfeld auf `TimetableRowData` (8 Flags pro Seite plus `userEnteredDwell`) als TTT-Export-Gate. Nur user-eingegebene Werte gehen in den Path Request — gespiegelte/abgeleitete/propagierte Werte bleiben lokal.
(3) **Day-Offset** (`arrivalOffset` / `departureOffset` Integer) für Mitternachts-Übertritte und Tag-(-1)-Modifications. Propagation rechnet in absoluten Minuten, so dass 23:50 + 30 min korrekt 00:20 mit Offset +1 ergibt.
(4) **`TimetableEditingService.applyHaltRules(row)`** erzwingt Regeln 1–7: Halt=false → alle Zeitfelder gestrippt; Dwell + 1 Seite → Same-Mode-Mirroring der anderen Seite; beide Seiten gefüllt → Dwell automatisch geleert; Origin/Destination → implizite Halte ohne Dwell, nur eine Seite erlaubt.
(5) **Forward-Fallback (Regel 7)**: Forward-STRETCH ohne expliziten Pin/Destination-Arrival fällt auf SHIFT zurück. Backward-STRETCH bekommt Origin als impliziten Anker.
(6) **Segmental Speed-Interpolation**: zwischen User-Anchor-Paaren wird Reise-Geschwindigkeit aus `Δtime / Δdistance` berechnet, davor/danach 70 km/h Default. Ein einziger Anker reicht für sinnvolle Estimates über die ganze Route.
(7) **Editor-Panel rule-driven**: Halt-Checkbox bei Origin/Destination ausgeblendet; Halt-Sektionen bei Halt=false versteckt; AFTER zeigt nur ELA-Picker, BEFORE nur LLA-Picker, WINDOW beide; Mode-Switch übernimmt vorhandenen Wert in neue Felder.
(8) **Grid-TTT-Markierung**: `ComponentRenderer` für An/Ab/Punkt/Dwell zeigt user-eingegebene Werte mit **TTT-Badge** in Akzent-Farbe + Bold. Origin: An-Zelle leer; Destination: Ab-Zelle leer. `+1d`/`-1d`-Suffix bei Day-Offset ≠ 0.
(9) **Tests**: `TimetableEditingServiceTest` 57/57 grün — Halt-Regeln, Mode-Mirroring, Mode-Switch-Preservation, segmentale Interpolation, Single-Anchor-Fringe-Fill, Mitternachts-Wraparound, AFTER/BEFORE-Anker-Extraktion, Endpoint-Regeln.
(10) **Doku**: ADR-010 komplett überarbeitet, Wiki und GLOSSARY.md erweitert.

Davor: **2026-05-05** — Doku-Konventionen formalisiert: (1) `package-info.java` fuer jeden aktiven Bounded Context (domain.business, domain.order, domain.timetable, domain.pathmanager, domain.customer, domain.userprefs, domain.infrastructure, domain.rollingstock) und fuer die Top-Layer-Pakete (ui, infrastructure). Jede Datei beschreibt Aggregate-Roots, Schluessel-Beziehungen mit @link-Refs, Authorization-Modell und Architektur-Constraint des Pakets. (2) `JavaDocCoverageTest` (JUnit, kein ArchUnit weil Kommentare bei Compile gestrippt werden) walkt `src/main/java` und prueft per Regex dass jede Klasse mit `@Service`/`@Entity`/`@Repository`/`@RestController`/`@Component`/`@Configuration`/`@SpringComponent` einen Klassen-JavaDoc-Header hat. Bricht den Build wenn jemand neue Service-/Entity-Klasse ohne Header anlegt. Fand zwei Lücken (RollingStockRepository, RollingStockService) — gefixt im selben Commit. Class-level only, keine Methoden-Boilerplate (entspricht CLAUDE.md-Regel). (3) Doku-Ebenen jetzt vollstaendig: Klasse → JavaDoc-Header (Test-erzwungen), Paket → `package-info.java`, Schicht → ArchUnit `ArchitectureTest`, Naming → ArchUnit `NamingConventionTest`, Architektur-Entscheidung → ADRs in `docs/decisions/`, lebender Stand → `PROJECT_STATUS.md`, Architektur-Diagramm → `ARCHITECTURE.md` (Mermaid), Datenmodell → `datenmodel.md`, Glossar → `GLOSSARY.md`, User-Anleitung → GitLab-Wiki.

Davor: **2026-05-04** — Master-Detail-Refactor `/orders` und `/businesses`, Keyboard-First UX, A11y-Baseline, Geschaeft <-> Bestellposition (PurchasePosition) M2M, generische User-View-Preferences, Resource->Bedarf Umbenennung. (1) **Generische `MasterDetailLayout<T>`** in `ui/component/masterdetail/` (Spec-Builder, ARIA `role="listbox"`, ↑/↓/Home/End Keyboard-Nav, Type-ahead-Filter, aria-live Selection-Announce). (2) **`/businesses` neu**: `BusinessOverviewView` (`@Route("businesses/:businessId")` + `@RouteAlias("businesses")`), `BusinessCard` (Status-Gutter, Icon+Text-Pill statt Farbe-only), `BusinessLinksTree` (vereinheitlichter Auftrag->AP->BP-Tree mit Checkbox-Spalte statt zwei separater Panels und Picker-Dialoge). `BusinessListView` geloescht; `BusinessDetailView` ohne `@Route`, embeddable. (3) **`/orders` neu**: `OrderOverviewView` (`/orders`, `/orders/{id}`, `/orders/{id}/positions/{posId}`), `OrderCard`, neue `OrderPositionDetailView` (Einzelposition mit Breadcrumb + "Zum Auftrag"-Button, validiert dass Position zum Auftrag gehoert -> verhindert Cross-Order-Deep-Links). `OrderListView`/`OrderAccordionRow`/`OrderRowCallbacks` geloescht; `OrderDetailView` mit `@SpringComponent` + Prototype-Scope, neue `setMode(orderId, isNew)`-API; instanziiert via `ObjectProvider`. (4) **A11y-Komponenten** in `ui/component/a11y/`: `SkipLinks` (visible-on-focus), `BreadcrumbBar` (`<nav aria-label>` + `aria-current="page"`), `AriaLive` (visually-hidden `role="status"`), `CommandPalette` (`Ctrl+K`, fuzzy ueber Auftraege+Geschaefte+Positionen), `KeyboardHelpOverlay` (`Shift+?`). Globale Shortcuts in `MainLayout` via Body-keydown: `g o`/`g b`/`g h` (vim-style), `Ctrl+K`, `Shift+?`. List-Shortcuts: `/` Filter, `Esc` clear, `n` neu, ↑/↓/Home/End/Enter. (5) **Geschaeft <-> Bestellposition**: V29-Migration (`business_purchase_positions` Join + Audit), `Business.purchasePositions` ManyToMany, `BusinessService.linkPurchasePosition`/`unlinkPurchasePosition`/`getLinkedPurchasePositions`/`getAllPurchasePositions` (mit JOIN FETCH zur Vermeidung von LazyInit), erweiterter `create(...)` mit Initial-Verknuepfungen. (6) **`UserViewPreference` (V30)** generisch: `(user_id, view_key) -> JSON`. `GridPreferenceBinder<T>` persistiert Spaltenreihenfolge/-breite/-sichtbarkeit (debounced 1.5s), `GridSettingsButton` mit Popover (Checkbox-Liste + Reset auf Standardansicht + Versteckt-Spalten-Badge). (7) **LazyInit-Fix in BusinessService**: `getLinked{Order,Purchase}Positions` und `getDocuments` geben jetzt `new ArrayList<>(...)` (detached) zurueck statt PersistentBag, `Hibernate.initialize` auf benoetigte Lazy-Felder. (8) **Draft-Modus fuer neues Geschaeft**: Dokumente und Positions-Verknuepfungen werden in-memory gepuffert und nach `create(...)` als zweiter Schritt persistiert (Layout: vertikaler Split, oben Form+Tree 65%, unten Dokumente in voller Breite). (9) **Resource->Bedarf Umbenennung** (i18n only): `resource.title` (Bedarfe), `resource.add` (Bedarf hinzufuegen), `resource.summary` (Bedarfe . Bestellpositionen), `purchase.add` (Bestellposition hinzufuegen) in DE/EN/FR/IT. Code-Modell unveraendert (`ResourceNeed` mit Coverage INTERNAL/EXTERNAL bleibt, `PurchasePosition` separat). (10) **Tests**: `BusinessServiceLinkPositionsTest` (3 Faelle: create-mit-OPs, link/unlink-after-create, getDocuments-detached). (11) **Codex-Review** (P2): OrderPositionDetailView prueft jetzt `orderId == pos.getOrder().getId()` (gefixt). Eigene Findings: tote Imports in `MasterDetailLayout` (BiConsumer/KeyModifier/UI), `CommandPalette` (UUID), `BusinessLinksTree` (GridSettingsButton), totes Feld `matcherAdapter` -> alle entfernt. (12) **ADR-014** dokumentiert Master-Detail + A11y + Routing-Schema; Pre-existing P1/P2-Bugs in `PathManagerService` und `VehiclePlanningView` von Codex separat aufgenommen, ausserhalb dieses Refactors.

Davor: **2026-03-31** — Codex Review Round 3 Fixes (6 Findings): (1) CRITICAL Audit Table Name Mismatch: Envers YAML-Property `org.hibernate.envers.audit_table_suffix` war falsch verschachtelt unter `hibernate:`, dadurch wurde der Default `_aud` statt `_audit` verwendet. Fix: Flat-Key-Syntax in application.yaml. V17/V18-Migrationen korrigiert: Non-PM-Audit-Tabellen verwenden jetzt `_audit` statt `_aud`. Neue V19-Migration: PM-Audit-Tabellen von `_aud` auf `_audit` umbenannt, Envers-Spalten von `rev`/`revtype` zurueck auf `revision_id`/`revision_type`, `journey_location_type` von VARCHAR(2) auf VARCHAR(20) geweitet. (2) HIGH LazyInitializationException: `PurchaseDialog` und `TttOrderDialog` akzeptieren jetzt primitive Konstruktor-Parameter (UUID, String, boolean) statt JPA-Entities. Keine lazy-loaded Entity-Zugriffe mehr im UI-Thread. `ResourcePanel`-Aufrufe entsprechend angepasst. (3) HIGH PurchaseOrderService.pmReferenceTrainId: `triggerTttOrder()` setzt jetzt `position.setPmReferenceTrainId(train.getId())` und speichert die Position, sodass der Link zwischen OrderPosition und PmReferenceTrain persistiert wird. (4) HIGH Stringly-typed Enums: `PmPathRequest.currentState` und `PmPath.currentState` von `String` auf `PathProcessState` enum mit `@Enumerated(EnumType.STRING)` umgestellt. Compile-time Safety statt Runtime-String-Vergleiche. (5) HIGH PathProcessController: `PathAction.valueOf()` Exception wird jetzt als `IllegalStateException` (400 Bad Request) statt `IllegalArgumentException` (404 Not Found) zurueckgegeben. (6) HIGH JourneyLocationType: `PmJourneyLocation.journeyLocationType` von `@Column(length=2)` auf `@Column(length=20)` geweitet, V19-Migration weitet die DB-Spalte. Alle 83 Tests bestanden.

Davor: **2026-03-31** — Audit Trail Gaps behoben + Audit History UI: (1) V18-Migration: `createdBy`/`updatedBy` VARCHAR(100) Spalten auf 12 Entities (order_positions, resource_needs, purchase_positions, resource_catalog_items, timetable_archives, pm_reference_trains, pm_train_versions, pm_path_requests, pm_paths, pm_routes, pm_timetable_years, pm_journey_locations) inkl. Audit-Tabellen (*_aud). (2) Java-Entities: `@CreatedBy`/`@LastModifiedBy` + `@EntityListeners(AuditingEntityListener.class)` auf allen 12 Entities. Entities die zuvor keine EntityListeners hatten (ResourceNeed, PurchasePosition) erhalten diese jetzt. (3) Neuer `AuditEntry` Record in Domain-Schicht (revision, timestamp, user, type, changes). (4) Neuer `AuditService` (@Transactional readOnly): Liest Envers-Revisionen via `AuditReaderFactory`, vergleicht Snapshots feldweise, erkennt geaenderte Felder, extrahiert createdBy/updatedBy aus Entity-Snapshots. Methoden: getOrderHistory, getPositionHistory, getResourceNeedHistory, getEntityHistory (generisch). (5) Neuer `AuditHistoryDialog` (Vaadin Dialog 700px, Grid mit Rev/Zeitpunkt/Benutzer/Aktion/Aenderungen). (6) UI-Integration: "Verlauf"-Button im Order-Header (OrderDetailView), Clock-Icon je Position (OrderPositionRow), Clock-Icon je Ressource (ResourcePanel). (7) i18n: 11 audit.*-Keys in DE/EN/IT/FR + Fallback-Bundle. (8) ArchUnit-konform: AuditEntry in domain.order.model, kein UI-Import aus Service. Alle 83 Tests bestanden.

Davor: **2026-03-31** — Gemini Architecture Review Fixes (3 High-Priority Items): (1) Proactive Graph Loading: `TimetableRoutingService.warmUpGraphCache()` mit `@EventListener(ApplicationReadyEvent.class)` laedt RINF Routing-Graph und OP-Map beim Start in den Cache statt lazy beim ersten Request. SLF4J-Logging mit Ladezeit in ms. (2) REST API Security: `SecurityConfig.apiSecurityFilterChain()` unterstuetzt jetzt konfigurierbare JWT-Authentifizierung via `app.api.security-enabled` Property (default: false fuer Dev/Demo, true in `application-prod.yaml`). Swagger/OpenAPI bleibt offen, API-Endpoints nutzen `oauth2ResourceServer(jwt)` wenn aktiviert, `/api/v1/pathmanager/**` erfordert ADMIN/DISPATCHER-Rolle. WARNING-Log bei Startup wenn Security deaktiviert. Neuer Eintrag in `.env.example`. (3) BeforeLeaveObserver: `TimetableBuilderView` implementiert `BeforeLeaveObserver` mit ConfirmDialog bei ungespeicherten Aenderungen (wenn Timetable-Rows existieren und Step != ROUTE). i18n-Keys `common.unsavedChanges` und `common.unsavedChanges.text` in DE/EN/IT/FR. Alle 83 Tests bestanden.

Davor: **2026-03-31** — Dokumentations-Gesamtupdate Ressourcen/Bestellungen: datenmodel.md, ARCHITECTURE.md, BEDIENUNGSANLEITUNG.md (Kap. 14), PROJECT_STATUS.md (V15-V17, neue Komponenten), GitHub Wiki (Resources-and-Purchases.md, Screenshots, Home/Sidebar Update).

Davor: **2026-03-31** — Codex Code Review + Pentest Findings behoben (9 Fixes): (1) CRITICAL V17 Migration: Auditierungstabellenname `purchase_positions_aud` verifiziert als korrekt — konsistent mit V2. False Positive. (2) HIGH ResourcePanel Stale Data: `findPurchasesForNeed()` laedt jetzt frisch via `PurchasePositionRepository.findByResourceNeedId()` statt aus dem stale `position`-Objekt. Neuer `refreshCallback` (Runnable) benachrichtigt Parent nach Mutationen (Sync, TTT-Trigger, Bestellen). (3) HIGH LazyInitializationException: Neuer `findByOrderPositionIdWithCatalogItem()` JOIN FETCH Query in ResourceNeedRepository, `getResourcesForPosition()` nutzt diesen. (4) HIGH triggerTttOrder erstellt PmPathRequest: Nach Erstellung des ReferenceTrains wird `PathProcessEngine.executeTransition(SEND_REQUEST)` aufgerufen, Train danach reloaded fuer korrekte pathRequestId. (5) MEDIUM Race Condition Position Numbers: `generatePositionNumber()` nutzt jetzt UUID statt count+1 (`BP-` + 8-Zeichen UUID). (6) MEDIUM Input Validation: `addResource()` validiert type, coverage (not null), quantity >= 1. (7) MEDIUM debitCode: `extractDebitCode()` parsed JSON und setzt `PurchasePosition.debicode`. (8) MEDIUM CSV Import transaktional: Neuer `ResourceCatalogImportService` mit `@Transactional` + `@PreAuthorize`, CatalogTab delegiert. (9) PENTEST @PreAuthorize: Auf `PredefinedTagImportService.importCsv()`, `RinfImportService.importOperationalPoints()` und `importSectionsOfLine()` hinzugefuegt. Alle 83 Tests bestanden.

Davor: **2026-03-31** — TTT Order Dialog: Neuer Dialog fuer CAPACITY-Bestellungen via TTT mit Pflichtfeldern (Debitcode, Kontakt, Brems-/Zugfolge) und erweiterten optionalen Attributen in 6 aufklappbaren Sektionen (Traktion, Kalender, NSP, Referenzen, erweiterter Kontakt, Sondertransport). (1) V17-Migration: JSONB-Spalte `ttt_order_attributes` auf `purchase_positions` und `purchase_positions_aud`. (2) PurchasePosition-Entity um `tttOrderAttributes` Feld erweitert. (3) Neuer `TttOrderDialog` (Vaadin Dialog, 700px, FormLayout 2-spaltig, Header mit Zugdaten aus OrderPosition, Footer mit Cancel/Bestellen). (4) `TttOrderDetailSections` als separate Klasse mit den 6 Detail-Sektionen (alle unter 300 Zeilen). (5) `PurchaseOrderService.triggerTttOrder()` ueberladen mit optionalem `tttAttributes` JSON-Parameter, speichert Attribute vor dem TTT-Trigger. (6) `PurchaseDialog`: Bei CAPACITY+TTT wird nach Erstellen der PurchasePosition der TttOrderDialog geoeffnet statt direkt zu bestellen. (7) `ResourcePanel`: TTT-Button oeffnet jetzt TttOrderDialog statt direkt triggerTttOrder aufzurufen. (8) i18n: 35 neue ttt.order.*-Keys in DE/EN/IT/FR + Fallback-Bundle. Alle 83 Tests bestanden.

Davor: **2026-03-31** — Vehicle Planning LazyInitializationException behoben + E2E-Tests erweitert: (1) Kritischer VP-Bug gefixt: `LazyInitializationException` beim Navigieren zu `/vehicleplanning` weil `PmReferenceTrain.trainVersions` und `VpVehicle.entries` lazy-loaded waren aber ausserhalb der Transaction im Vaadin UI-Thread zugegriffen wurden. Fix: Neue Query `findByYearWithVersions()` in `PmReferenceTrainRepository` (nur trainVersions JOIN FETCH, kein pathRequests wegen MultipleBagFetchException). Neue Query `findByRotationSetIdWithEntries()` in `VpVehicleRepository` (Vehicles mit Entries und ReferenceTrains eager-loaded). `GanttChart.resolveTrainTimes()` mit aeusserem try/catch fuer LazyInitializationException als Safety-Net. (2) S-Bahn E2E-Test um 4 neue Tests erweitert: `09d2` (Dienst-Wochen FLIRT Woche 1+2 erstellen), `09d3` (Gantt-Chart-Rendering verifizieren: gantt-element Tag, 1751x600px), `09d4` (Von/Fuer-Button klicken mit Notification-Check), `09d5` (Von/Fuer-Daten via REST API in PM-Locations verifizieren). Alle 22 E2E-Tests bestanden.

Davor: **2026-03-31** — Vehicle Planning Gantt Rewrite: GanttChart komplett neu geschrieben mit tltv Gantt Flow Add-on (org.vaadin.addons.tltv.gantt:gantt-flow-addon:24.0.2). TrainPalette entfernt, stattdessen "Shelf"-Zeilen (Ablage) im Gantt fuer nicht-zugewiesene Zuege mit greedy bin-packing Algorithmus. VehiclePlanningView vereinfacht: kein SplitLayout mehr, volle Breite Gantt, FPJ-ComboBox (Fahrplanjahr-Auswahl), "Dienst hinzufuegen"-Dialog. VehiclePlanningService erweitert um getUnassignedTrains() und addVehicle(). VpRotationEntryRepository um findByVehicleRotationSetId(). i18n: 5 neue Keys (vp.shelf, vp.duty, vp.addDuty, vp.fpj, vp.duty.created) in DE/EN/IT/FR. Gantt-Architektur: Step=Zeile (Shelf/Duty), SubStep=Zugblock, DnD zwischen Zeilen via setMovableStepsBetweenRows(true), StepMoveEvent Handler. Drag-to-shelf = Zuweisung entfernen, Drag-to-duty = Zug zuweisen. Alle Dateien unter 340 Zeilen.

Davor: **2026-03-31** — Dokumentations-Gesamtupdate: Alle Dokumentation aktualisiert fuer Vehicle Planning, TTR Phase Resolver, S-Bahn E2E Test, V12-V14 Migrationen. Neue Dateien: wiki/vehicle-planning.md, ADR-012 (VP Gantt), ADR-013 (TTR Automation). Aktualisiert: datenmodel.md (+VP ER-Diagramm, +TTR Phasen), ARCHITECTURE.md (+VP Context, +TtrPhaseResolver), BEDIENUNGSANLEITUNG.md (+Kap. 13, +TTR in Kap. 7), path-manager.md (+TTR-Phasen), GLOSSARY.md (+12 Begriffe), PROJECT_STATUS.md (V12-V14, Tests, ADRs, Changelog).

Davor: **2026-03-31** — TTR Phase / TTT Process Type Mapping: Automatische Zuordnung der TTR-Phase (Capacity Strategy, Capacity Model, Capacity Supply, Annual Ordering, Late Ordering, Ad Hoc, Past) zum TTT ProcessType basierend auf Fahrplanjahr-Startdatum. (1) Neuer Enum `TtrPhase` mit 7 Phasen und Timeline/Label-Metadaten. (2) Neuer Service `TtrPhaseResolver` mit 4 Methoden (resolvePhase, resolveProcessType, isDraftOfferAllowed, phaseDescription). (3) `PathProcessEngine` erweitert: Phase-basierte Filterung der verfuegbaren Aktionen (kein IM_DRAFT_OFFER in Bestellphase 3), dynamische Validierung fuer IM_FINAL_OFFER aus RECEIPT_CONFIRMED, neue Methoden resolvePhaseForTrain/resolveProcessTypeForTrain. (4) `PathManagerView`: TTR-Phasen-Badge (farbcodiert: gruen=Annual, gelb=Late, orange=AdHoc, grau=Past) und Phasen-Info bei Fahrplanjahr-Auswahl. (5) `ProcessSimulationPanel`: Phase-Info-Box bei NEW-Zustand mit ProcessType-Hinweis und Bestellphase-3-Warnung. (6) V14-Migration: Seed fuer FPJ 2025 und 2027 (2026 existiert bereits). (7) i18n: 10 ttr.*-Keys in DE/EN/IT/FR. (8) Unit Test: TtrPhaseResolverTest mit 25 Tests fuer alle Phasen und Grenzwerte. Alle 39 Tests bestanden.

Davor: **2026-03-31** — Fahrzeugumlaufplanung (Vehicle Planning): Neuer Bounded Context `vehicleplanning` mit komplettem Domain Layer, UI und Navigation. (1) V11-Migration: 4 Tabellen (`vp_rotation_sets`, `vp_vehicles`, `vp_rotation_entries`, `vp_vehicle_operations`) mit FK auf `pm_timetable_years` und `pm_reference_trains`, 6 Indexes. (2) Domain Model: 2 Enums (`VehicleType`, `CouplingPosition`), 4 Entities (`VpRotationSet`, `VpVehicle`, `VpRotationEntry`, `VpVehicleOperation`) ohne `@Audited` (Planungsdaten), 1 Conflict-Record. (3) 4 Repositories. (4) 2 Services: `VehiclePlanningService` (CRUD, addTrainToVehicle, moveEntry, removeEntry) und `ConflictDetectionService` (Zeitueberlappung und Standort-Mismatch-Erkennung ueber PmJourneyLocation). (5) UI: `GanttChart` (Div-basiert mit Zeitlineal, Fahrzeugzeilen, absolut positionierte Zugbloecke, DragSource/DropTarget), `TrainPalette` (Sidebar mit Suchfeld und draggbaren PM-Zuegen), `ConflictPanel` (Konfliktliste mit Severity-Icons). (6) `VehiclePlanningView` unter Route `/vehicleplanning` mit Umlaufplan-Auswahl, Tagesauswahl, Neuer-Umlaufplan-Dialog, SplitLayout 20/80 (Palette/Gantt), Konfliktpanel. (7) MainLayout: Navigation und Breadcrumb ergaenzt. (8) i18n: 30 vp.*-Keys in DE/EN/IT/FR.

Davor: **2026-03-31** — Timetable Builder 6 UX-Fixes: (1) Zeitfenster-Labels eindeutig: 4 neue i18n-Keys fuer kontextspezifische Labels (earliestArrival/ELA, latestArrival/LLA, earliestDeparture/ELD, latestDeparture/LLD) in DE/EN/IT/FR. (2) Grid-Spalten reduziert von 14 auf 8: Entfernt Role, From, To, Segment, Distance und getrennte Schaetzung/Constraint-Spalten; neu kombinierte An/Ab-Spalten mit Qualifier-Tags, Halt-Spalte mit Haekchen, Dwell-Spalte. (3) PM Zeitfeld-Labels: JourneyLocationPanel passt die Zeitfeld-Beschriftung dynamisch an den gewaehlten Qualifier an (ELA/LLA/ELD/LLD), korrekte Qualifier-Items fuer Arrival/Departure. (4) Route-Zeiten optional: Validation erlaubt Route-Berechnung ohne Ankerzeit (rein geographisch); TimetableRoutingService.estimateRows gibt Zeilen ohne Zeiten zurueck wenn keine Anker. Laufweg-Vorschau als kompakte Punkt-Liste nach Routenberechnung. (5) Kompakter Kalender: ValidityCalendar.setCompact() fuer Einzeilen-Monate mit 18x18px-Tages-Quadraten; TimetableTableStep nutzt compact-Modus. (6) Gueltigkeit im Builder-Header: Auftrags-Gueltigkeitsdaten (dd.MM.yyyy) im Subtitle angezeigt.

Davor: **2026-03-31** — Dokumentations-Ueberarbeitung: (1) Bedienungsanleitung (BEDIENUNGSANLEITUNG.md) als vollstaendiges deutsches Benutzerhandbuch erstellt (~600 Zeilen, 12 Kapitel: Ueberblick, Anmeldung/Navigation, Auftragsverwaltung, Positionen, Builder, Detailansicht, Fahrplanmanager, Einstellungen, Profil, Tastaturkuerzel, FAQ, Fehlerbehebung). (2) 6 fehlende ADRs erstellt: ADR-004 (Accordion+Status-Chips), ADR-005 (Order-Header+Dialoge), ADR-006 (Bestellkalender/TTR), ADR-007 (ERA RINF), ADR-008 (CSV-Tag-Import), ADR-009 (Fahrplan-Archiv-Link). (3) README.md um Dokumentations-Links erweitert. (4) timetable-builder.md um Tastaturkuerzel und Fehlerbehebung ergaenzt. (5) path-manager.md um Swagger-UI-Anleitung und haeufige Fehler ergaenzt. (6) GLOSSARY.md um 5 Begriffe erweitert (Bestellkalender, Benutzerrolle, Theme, Audit Trail, Filter).

Davor: **2026-04-02** — Security Pentest Findings behoben: (1) CRITICAL: SecurityConfig `web.ignoring()` durch separate `@Order(1) SecurityFilterChain` fuer `/api/**` und Swagger ersetzt (stateless, CSRF-disabled). (2) HIGH: `@EnableMethodSecurity` aktiviert, `@PreAuthorize("hasAnyRole('ADMIN', 'DISPATCHER')")` auf OrderService Mutations (save, delete, savePosition, deletePosition). (3) HIGH: Default-Credentials in `.env.example` und `docker-compose.yml` durch `CHANGE-ME-in-production` / `dev-only-change-me` ersetzt mit WARNING-Kommentaren. (4) MEDIUM: Error-Handler in PathManagerController, PathProcessController, PathManagerDiffController sanitisiert (generische Fehlermeldungen statt Exception-Details). (5) MEDIUM: `SecurityAuditorAware` (AuditorAware<String>) implementiert, AuditConfig referenziert. (6) MEDIUM: `PathProcessEngine.executeTransition()` setzt `simulatedBy` aus SecurityContext. (7) MEDIUM: GitHub Actions SHA-Pinning (checkout, setup-java, upload-artifact, gitleaks). (8) LOW: SSL-Hinweis in application.yaml, Security-Logging von DEBUG auf INFO in dev-Profil. Alle 58 Tests bestanden.

Davor: **2026-04-02** — Code Review Backlog: 6 verbleibende Findings behoben. (1) i18n Fallback Bundle (`messages.properties`) mit allen Keys aus `messages_en.properties` synchronisiert. (2) Route Graph Caching: `TimetableRoutingService` cached OP-Map und Graph als Instanzfelder mit 5-Minuten-TTL statt bei jedem `calculateRoute`/`routeFromStoredRows` neu zu laden. (3) Google Fonts CDN-Import entfernt (GDPR), stattdessen lokale System-Fonts mit Fallbacks (`'Segoe UI'`). (4) Leaflet Map: `destroy()`-Methode fuer permanentes Aufraumen hinzugefuegt (Memory Leak Prevention), `disconnectedCallback` bleibt non-destructive fuer Vaadin Re-Attach. (5) Dead Code: `StatusBadge` — ueberfluessige rgba-Background-Berechnung entfernt, durch `color-mix` ersetzt. (6) File Size: `OrderAccordionRow` (~390 Zeilen) aus `OrderListView` (722 Zeilen) in eigene Klasse `ui/component/OrderAccordionRow.java` extrahiert; OrderListView jetzt 328 Zeilen. Alle 58 Tests bestanden.

Davor: **2026-04-02** — Codex Code Review: 14 Findings behoben (2 Critical, 7 High, 5 Medium). (1) SecurityConfig: `web.ignoring("/api/**")` eingeschraenkt auf `/api/v1/pathmanager/**` + Swagger. (2) IDOR-Fix: PathManagerController validiert Version-zu-Train und Location-zu-Version Zugehoerigkeit. (3) State Machine: MODIFIED-Zustand hat jetzt Transitionen (WITHDRAW, IM_RECEIPT, IM_DRAFT_OFFER, IM_NO_ALTERNATIVE). (4) updateTrainHeader: Null-Felder ueberschreiben nicht mehr bestehende Werte. (5) DiffController: @Transactional(readOnly=true) gegen LazyInitializationException. (6) JSON-Erzeugung in PathManagerService: StringBuilder durch Jackson ObjectMapper ersetzt. (7) Stretch-Logik: Offset wird jetzt korrekt vom alten Anker berechnet und auf neuen Anker skaliert. (8) N+1: findByYearWithDetails JOIN FETCH Query im Repository. (9) TimetableArchiveView: Ownership-Check (Position gehoert zum Auftrag). (10) Bean Validation: Request-Validierung im Controller. (11) DiffService: Composite Key (UOPID+Sequence) statt nur UOPID. (12) ddl-auto: update -> validate im Dev-Profil. (13) JourneyLocationPanel: Track und LocationType werden jetzt gespeichert (updateJourneyLocation erweitert). (14) Halt-Validierung: Akzeptiert jetzt geschaetzte Zeiten wenn TimeConstraintMode.NONE. Alle 52 Tests bestanden.

Davor: **2026-03-31** — Path Manager E2E Test + REST API Bugfix + Wiki Screenshots + finale Dokumentation. (1) Neuer E2E-Test `path-manager.spec.ts` mit 8 Tests: Sidebar-Navigation, TreeGrid-Anzeige (FPJ 2026), Train-Submit via REST, TreeGrid-Expansion, Available-Actions-Pruefung, SEND_REQUEST-Transition, API-List/Detail-Endpoints, Cleanup-Verifikation. (2) REST API Bugfix: `LazyInitializationException` in `PathManagerController` behoben durch `@Transactional(readOnly = true)` auf Klasse + `@EntityGraph` fuer Eager-Loading der JourneyLocations in `PmTrainVersionRepository`. (3) Wiki-Screenshots aktualisiert: `path-manager-view.png`, `path-manager-tree-expanded.png`, alle Timetable-Screenshots aus frischem Comprehensive-Test-Run. (4) Teststand: 49 E2E + 45 Unit + 31 ArchUnit = 125 Tests gesamt.

Davor: Timetable Builder UX: 3 Fixes umgesetzt. (1) Route berechnen bleibt auf Step 1, zeigt Erfolgs-Notification und gruenen Button; "Next" wechselt zu Step 2 wenn Route existiert. (2) Interaktive Karte mit OP-Hintergrund-Markern: alle Betriebspunkte als kleine graue Kreise auf der Karte, Klick fuellt Von/Nach/Via-Felder sequentiell. (3) Kompakter Kalender in Step 2: Gueltigkeit als zusammenklappbares Details-Akkordeon mit Scrollhoehe 200px, standardmaessig geoeffnet wenn keine Daten gewaehlt. i18n-Keys `timetable.route.calculated` und `timetable.route.calculated.summary` in DE/EN/IT/FR. E2E-Tests angepasst.

Davor: Path Manager Dokumentation umfassend erweitert: datenmodel.md mit ER-Diagramm, detaillierten Feldtabellen (alle 8 Entities), vollstaendiger TTT State Machine (Mermaid stateDiagram), Zustandsbeschreibungen, Aktionen/Rollen-Tabelle, Integrationsmodell (Datenfluss-Diagramm) und REST-API-Endpunkt-Tabelle. ARCHITECTURE.md mit Komponenten-Diagramm, PathProcessEngine-Pattern-Beschreibung und API-Summary. Wiki-Seite docs/wiki/path-manager.md (User Guide mit TreeGrid-Erklaerung, Submissions-Anleitung, 3 Beispielflows, Zugkopf/OP-Bearbeitung, Diff-Feature, API-Referenz). ADR-011 (Path Manager Architektur: REST API als Integrations-Boundary, Shared DB mit pm_-Prefix, statische State Machine, Simulationsansatz, Alternativen: Microservice/Direct Injection/Message Queue). GLOSSARY.md um 16 Path-Manager-Begriffe erweitert (Fahrplanmanager, Referenzzug, Trasse, Trassenantrag, Laufweg, Zugversion, Prozessschritt, State Machine, RA, IM, Draft/Final Offer, Booked, Fahrplanjahr, TRID, PAID, PRID, ROID).

Davor: Path Manager Integration Button: "Send to Path Manager" Button in `OrderPositionRow` fuer FAHRPLAN-Positionen (Train-Icon, orange wenn nicht gesendet, teal wenn gesendet). `OrderPositionPanel` ruft `PathManagerService.createTrainFromOrderPosition()` auf, speichert `pmReferenceTrainId` auf der Position, zeigt Erfolgs-Notification. Nach Senden wechselt der Button zu "View in PM" und navigiert zum PathManagerView. Neue Dependencies in `OrderDetailView` durchgereicht. i18n in DE/EN/IT/FR. Doku in datenmodel.md, timetable-builder.md und ARCHITECTURE.md aktualisiert.

Davor: Path Manager Vaadin UI umgesetzt: `PathManagerView` mit SplitLayout (40/60), TreeGrid mit sealed-interface `TreeNode` (YearNode, TrainNode, VersionNode, LocationNode) und lazy-loading DataProvider. Rechte Seite kontextabhaengig: `TrainHeaderPanel` (Zugdaten-Formular), `ProcessSimulationPanel` (TTT State Machine mit RA/IM-farbcodierten Aktionsbuttons, Kommentarfeld, Prozesshistorie-Grid), `JourneyLocationPanel` (OP-Bearbeitung), Versionsdetail mit Location-Grid. Navigation in MainLayout ergaenzt (VaadinIcon.TRAIN), Breadcrumb-Support. i18n in DE/EN/IT/FR (40+ Keys).

Davor: Path Manager REST API umgesetzt: 9 DTOs in `dto.pathmanager` (TrainSubmitRequest, JourneyLocationDto, TrainSummaryDto, TrainDetailDto, TrainVersionDto, ProcessTransitionRequest, ProcessStepDto, AvailableActionsDto, DiffResultDto), Mapper in `mapper.pathmanager` (PathManagerDtoMapper), 3 REST Controller in `api.pathmanager` (PathManagerController mit 7 Endpoints, PathProcessController mit 3 Endpoints, PathManagerDiffController mit 1 Endpoint). Springdoc/Swagger annotiert. Kompiliert sauber, Spotless angewendet.

Davor: Path Manager Domain Layer komplett umgesetzt: V9-Migration mit 8 `pm_*` Tabellen + Audit-Tabellen + Indexes, FK-Erweiterung auf `order_positions`, Seed-Daten fuer FPJ 2026. Entities (`PmReferenceTrain`, `PmTrainVersion`, `PmJourneyLocation`, `PmPath`, `PmPathRequest`, `PmRoute`, `PmTimetableYear`, `PmProcessStep`), 4 Enums (`PathProcessState`, `PathAction`, `PathProcessType`, `VersionType`), 8 Repositories, 4 Services (`PathProcessEngine` mit vollstaendiger TTT State Machine, `PathManagerService` CRUD, `IdentifierGenerator`, `DiffService`).

Davor: Read-only Timetable Archive View (Fahrplan-Detailansicht) unter Route `orders/:orderId/timetable/:positionId`. SplitLayout 65/35: links Div-basierte Fahrplantabelle mit Farbcodierung (Origin/Destination amber, Halte teal, Durchfahrten gedaempft, Soft-Delete durchgestrichen), rechts Karte + Gueltigkeit + Metadaten. View-Button (Auge) in OrderPositionRow fuer FAHRPLAN-Positionen. i18n in DE/EN/IT/FR.

Davor: OTN-Feld (Operational Train Number) dokumentiert: Freitext VARCHAR 20 auf `timetable_archives` und `order_positions`, V8-Migration, Anzeige in Positionslisten/-kacheln, TTT-Mapping.

Davor: Fahrplanbuilder Phase 2-4: Timetable-Editing Features implementiert. TimetableTableStep erneut aufgeteilt (888 -> 367+507+140 Zeilen): AddStopForm und TimetableRowEditorPanel als eigene Klassen extrahiert. Stop Add/Remove mit Inline-Form und Soft-Delete, Shift/Stretch-Zeitpropagation, Commercial-Zeitmodus (PLA/PLD), TimingQualifier-Tags, Pinned-Zeiten, Activity-Pflicht bei Halt mit visueller Hervorhebung.

## Projektstatus

**Order CRUD + Auftragspositionen (`LEISTUNG` / `FAHRPLAN`) + Fahrplanbuilder + Bestellkalender + RINF Import + Schlagwort-Katalog + Path Manager**

## Module / Bounded Contexts

| Modul | Status | Entities | Views / Komponenten | Bemerkung |
|---|---|---|---|---|
| **Order** | CRUD aktiv + Ressourcen/Bestellungen | `Order`, `OrderPosition`, `ResourceNeed`, `PurchasePosition`, `ResourceCatalogItem` + Status-/Typ-Enums (`PurchaseStatus`, `ResourcePriority`, `ResourceOrigin`) | `OrderListView`, `OrderDetailView`, `OrderFormPanel`, `OrderPositionPanel`, `ServicePositionDialog`, `ResourcePanel`, `ResourceDialog`, `PurchaseDialog`, `TttOrderDialog`, `TttOrderDetailSections` | Auftragsuebersicht mit Status-Chips, Detailansicht mit angereicherten Positionszeilen, Leistungspositionen als Dialog, Ressourcen-Panel unter jeder Position, TTT-Bestellformular mit Pflicht-/Experten-Feldern, Ressourcen-Katalog im Settings-Bereich |
| **Timetable** | Aktiv | `TimetableArchive`, `TimetableRowData`, `TimetableEditingService`, `TttDraftBuilder`, Routing- und TTT-Enums | `TimetableBuilderView`, `TimetableRouteStep`, `TimetableTableStep`, `TimetableRowEditorPanel`, `AddStopForm`, `TimetableFormatUtils`, `TimetableMap` | Builder mit Stop Add/Remove (Inline-Form + Soft-Delete), Shift/Stretch-Propagation, Commercial-Modus (PLA/PLD), TimingQualifier-Tags, Pinned-Zeiten, TTT-Draft-Feldern fuer Ortstyp, mehrere Activities, Subsidiary und NSP |
| **Infrastructure** | Import aktiv | `OperationalPoint`, `SectionOfLine`, `ImportLog`, `PredefinedTag` | `SettingsView`, `TagsTab`, `TopologyTab`, `CatalogTab` | ERA RINF: 12.298 OPs + 13.849 SoLs (CH+DE), CSV-Import fuer Schlagwoerter, Ressourcen-Katalog Import (CatalogTab), synthetische Grenzverbinder fuer Routing |
| **Customer** | Entity | `Customer`, `CustomerStatus` | — | Repository vorhanden, eigene UI noch offen |
| **Business** | Entity | `Business`, `BusinessStatus` | — | Repository vorhanden, eigene UI noch offen |
| **Path Manager** | Domain + REST API + UI + Integration + TTR Phases | `PmReferenceTrain`, `PmTrainVersion`, `PmJourneyLocation`, `PmPath`, `PmPathRequest`, `PmRoute`, `PmTimetableYear`, `PmProcessStep` + 5 Enums (inkl. `TtrPhase`) | `PathManagerView`, `TrainHeaderPanel`, `ProcessSimulationPanel`, `JourneyLocationPanel`, `TreeNode` | TTT State Machine mit TTR-Phase-Mapping, CRUD, Diff, Identifier-Generator. V9+V14-Migration. REST API: 11 Endpoints. UI: TreeGrid + Detail Panels mit Prozess-Simulation + Phasen-Badge. Integration: Send-to-PM Button in OrderPositionRow. |

## Infrastruktur

| Komponente | Status | Details |
|---|---|---|
| **Vaadin** | 24.7.4 | Aktueller UI-Stand fuer Orders, Settings und Builder |
| **Security (Keycloak)** | Produktionsreif | OIDC + Realm Role Mapping, `@RolesAllowed` aktiv |
| **Rollen** | Verifiziert | `ADMIN` fuer Settings, Dispatcher und Viewer ohne Admin-Rechte |
| **i18n** | Aktiv | DE/EN/IT/FR, inkl. Builder, Settings, Order-Views |
| **Push / Live Updates** | Skeleton | `BroadcastService` und `@Push` vorhanden |
| **Audit Trail** | Aktiv | Hibernate Envers fuer Orders, Positionen, Ressourcen, Archive |
| **Datenbank** | V1-V31 Migrationen | Orders, Positionen, Ressourcen, Bestellungen, Infrastruktur, Schlagwoerter, Positionskommentare, Fahrplanarchiv, OTN, Path Manager (8 `pm_*` Tabellen + Audit + Multi-Year Seed + Country-Code-Fix V12/V13), historische `vp_*` Tabellen fuer Flyway-Kompatibilitaet, TTR-Seed (FPJ 2025/2026/2027), Ressourcenkatalog, TTT-Bestellattribute, Business-/UserPrefs-Erweiterungen, `pm_journey_locations.ttt_payload` fuer den TTT-Draft-Datenstrom |
| **Theme** | Aktiv | Profile-basiertes Theme mit sofortigem Umschalten; Fallback auf Default abgesichert |
| **Accessibility** | Aktiv | ARIA-Labels, Tastaturbedienbarkeit, lesbare Statuschips, Builder und Liste testbar |
| **Laufzeit** | Lokal verifiziert | Anwendung laeuft ohne Docker auf `*:8085` |

## Quality Gates

| Tool | Status | Beschreibung |
|---|---|---|
| **Spotless** | Aktiv | Google Java Style (AOSP) |
| **ArchUnit** | Aktiv | DDD-Schichtregeln, Naming, Annotations |
| **JaCoCo** | Aktiv | Mindest-Coverage im Build |
| **SpotBugs** | Aktiv | Statische Analyse |
| **OWASP Dep Check** | Aktiv | CVE-Scanning bei PRs |
| **Gitleaks** | Aktiv | Secret Scanning |
| **Playwright** | Aktiv | 49+ E2E-Tests in 8 Spec-Dateien + 14 S-Bahn E2E-Integration-Tests: Login, Order CRUD/Lifecycle, Timetable Builder/Editing/Comprehensive, Path Manager, S-Bahn Olten-Aarau Full Integration (14 Tests: Order, Route, Halte, Zeitfenster, Interval, PM, Archive, VP, Cleanup) |

## UI

| View | Route | Status | Beschreibung |
|---|---|---|---|
| **MainLayout** | — | Implementiert | Drawer-Nav, Breadcrumbs, Sprach- und Theme-Kontext |
| **LoginView** | `/login` | Implementiert | Keycloak SSO Redirect |
| **DashboardView** | `/` | Implementiert | KPI-Cards (Platzhalter) |
| **OrderListView** | `/orders` | Implementiert | Accordion + Summary-Metriken + Kommentarzeile + Status-Chips mit Zaehlern und Positionsfilter |
| **OrderDetailView** | `/orders/{id}` | Implementiert | Kompakter Header + Auftragspositionen + Bestellkalender |
| **TimetableBuilderView** | `/orders/{orderId}/timetable-builder` | Implementiert | Full-screen Fahrplanbuilder mit Route, Karte, Tabelle und Archiv-Save |
| **TimetableArchiveView** | `/orders/{orderId}/timetable/{positionId}` | Implementiert | Read-only Fahrplan-Detailansicht mit Div-Tabelle, Karte, Gueltigkeit und Metadaten |
| **PathManagerView** | `/pathmanager` | Implementiert | SplitLayout 40/60: TreeGrid (Year/Train/Version/Location) + kontextabhaengige Detail-Panels (Zug-Header, Prozess-Simulation, OP-Bearbeitung) |
| **SettingsView** | `/settings` | Implementiert | Topologie-Import + Schlagwort-Katalog + Datenbestand + Import-Verlauf (ADMIN only) |

## UI-Komponenten

| Komponente | Beschreibung |
|---|---|
| `OrderFormPanel` | Auftragsformular mit vordefiniertem Schlagwort-Katalog fuer `ORDER` / `GENERAL` |
| `ServicePositionDialog` | Dialog fuer `LEISTUNG`-Positionen mit OP-Auswahl, Zeitfeldern, Gueltigkeit, Tags und Kommentar |
| `OrderPositionPanel` | Einstieg fuer neue `LEISTUNG`- und `FAHRPLAN`-Positionen |
| `OrderPositionRow` | Angereicherte Positionszeile in der Auftragsbearbeitung mit Route, Zeitfenster, Service-Typ, Tags, Kommentar, View-Button (Auge) und Send-to-PM-Button (Zug) fuer FAHRPLAN |
| `PositionTile` | Positions-Kachel in der Auftragsliste mit Route, Zeitfenster, Kommentar, Tags, Bestellanzahl und Status |
| `PurchaseCalendarPanel` | Bestellkalender mit Summary, Grid und Details pro Position |
| `TimetableRowEditorPanel` | Rechte Seite des Table-Steps: Zeitbearbeitung, Halt/Aktivitaet, Propagation, Commercial-Modus, TTT-Ortstyp, TTT-Relevanz, mehrere Activities, Subsidiary und NSP |
| `AddStopForm` | Inline-Formular zum Hinzufuegen eines neuen Halts nach einer Zeile |
| `TimetableArchiveTable` | Read-only Div-basierte Fahrplantabelle mit Farbcodierung fuer Origin/Destination, Halte, Durchfahrten und Soft-Delete |
| `TimetableArchiveSidebar` | Rechte Seite der Archivansicht: Karte, Gueltigkeit, Metadaten |
| `TimetableMap` | Leaflet-/OpenStreetMap-Komponente fuer die Fahrplanroute mit klickbaren OP-Hintergrund-Markern |
| `TagsTab` | Schlagwort-Katalog mit CSV-Import im Settings-Bereich |
| `TopologyTab` | RINF-Import und Datenbestand im Settings-Bereich |
| `TreeNode` | Sealed-Interface mit YearNode, TrainNode, VersionNode, LocationNode fuer heterogenen TreeGrid |
| `TrainHeaderPanel` | Editierbares Formular fuer Zugdaten (OTN, Typ, Gewicht, Laenge, Geschwindigkeit, Kalender) |
| `ProcessSimulationPanel` | TTT-Prozesssimulation: Status-Badge, RA/IM-farbcodierte Aktionsbuttons, Kommentar, Prozesshistorie-Grid |
| `JourneyLocationPanel` | Editierbares Formular fuer Betriebspunkte (Zeiten, Qualifier, Aktivitaeten, Gleis) |
| `ResourcePanel` | Ressourcen-Panel unterhalb jeder Position: zeigt ResourceNeeds mit Typ, Katalog-Item, Menge, Prioritaet und zugehoerige PurchasePositions |
| `ResourceDialog` | Dialog zum Hinzufuegen neuer Ressourcenbedarfe mit Typ, Deckungsart, Katalog-Auswahl, Menge und Beschreibung |
| `PurchaseDialog` | Dialog zum Erstellen von Bestellpositionen mit Gueltigkeit und Beschreibung |
| `TttOrderDialog` | TTT-Bestellformular mit Pflichtfeldern (Debitcode, Kontakt, Bremsart, Zugfolge) und 6 aufklappbaren Experten-Sektionen |
| `TttOrderDetailSections` | Separate Klasse mit den 6 aufklappbaren Detail-Sektionen des TttOrderDialogs (Traktion, Kalender, NSP, Referenzen, Kontakt, Sondertransport) |
| `CatalogTab` | Ressourcen-Katalog-Tab im Settings-Bereich: Anzeige und CSV-Import von Fahrzeugtypen und Personalqualifikationen |

## Datenbank-Migrationen

| Version | Datei | Inhalt |
|---|---|---|
| V1 | `V1__create_schema.sql` | `orders`, `orders_audit`, `revinfo` |
| V2 | `V2__expand_domain_model.sql` | `customers`, `order_positions`, `resource_needs`, `purchase_positions`, `businesses` + Audit |
| V3 | `V3__infrastructure_tables.sql` | `operational_points`, `sections_of_line`, `import_log` |
| V4 | `V4__simplify_orders.sql` | `validFrom` / `validTo` statt Fahrplanjahr-Label |
| V5 | `V5__predefined_tags.sql` | `predefined_tags` Tabelle fuer Schlagwort-Katalog |
| V6 | `V6__position_comment.sql` | Kommentarspalte fuer `order_positions` und Audit |
| V7 | `V7__timetable_archive_and_border_connectors.sql` | `timetable_archives`, Audit, FK von `resource_needs`, 0m-Grenzverbinder fuer CH/DE-Routing |
| V8 | `V8__timetable_otn.sql` | `operational_train_number` (VARCHAR 20, nullable) auf `timetable_archives` und `order_positions` |
| V9 | `V9__path_manager_tables.sql` | `pm_timetable_years`, `pm_reference_trains`, `pm_routes`, `pm_path_requests`, `pm_paths`, `pm_train_versions`, `pm_journey_locations`, `pm_process_steps` + Audit-Tabellen + FK `pm_reference_train_id` auf `order_positions` + Seed FPJ 2026 |
| V10 | `V10__fix_pm_audit_columns.sql` | Envers Audit-Spalten `revision_id` -> `rev`, `revision_type` -> `revtype` in allen `pm_*_aud` Tabellen |
| V11 | `V11__vehicle_planning_tables.sql` | `vp_rotation_sets`, `vp_vehicles`, `vp_rotation_entries`, `vp_vehicle_operations` + 6 Indexes |
| V12 | `V12__fix_pm_country_code_length.sql` | `pm_journey_locations.country_code_iso` VARCHAR(2) -> VARCHAR(5) (+ aud) |
| V13 | `V13__fix_pm_country_code_aud.sql` | Nachtrags-Fix fuer Audit-Tabelle `country_code_iso` |
| V14 | `V14__ttr_multi_year_seed.sql` | Seed fuer FPJ 2025 und 2027 (ON CONFLICT DO NOTHING) |
| V15 | `V15__vehicle_linking_otn.sql` | `associated_train_otn` VARCHAR(20) auf `pm_journey_locations` und Audit-Tabelle |
| V16 | `V16__resource_catalog_and_extensions.sql` | `resource_catalog_items` + Audit, ResourceNeed-Erweiterungen (description, quantity, catalog_item_id, valid_from, valid_to, priority, origin), PurchasePosition-Erweiterungen (description, pm_path_request_id, pm_path_id, pm_process_state, pm_ttr_phase, pm_last_synced), Seed-Daten (6 Fahrzeugtypen + 4 Personalqualifikationen) |
| V17 | `V17__ttt_order_attributes.sql` | JSONB-Spalte `ttt_order_attributes` auf `purchase_positions` und `purchase_positions_aud` |
| V31 | `V31__pm_journey_location_ttt_payload.sql` | JSONB-Spalte `ttt_payload` auf `pm_journey_locations` und Audit-Tabelle fuer den verlustarmen TTT-Draft-Datenstrom |

## ERA RINF Infrastrukturdaten

| Land | Operational Points | Sections of Line |
|---|---|---|
| Schweiz (CHE) | 3.261 | 1.588 |
| Deutschland (DEU) | 9.037 | 12.261 |
| **Total** | **12.298** | **13.849** |

Quelle: ERA SPARQL Endpoint `https://era.linkeddata.es/sparql`

Hinweise:

- `operational_points_de.csv` enthaelt 16.060 Rohzeilen; der Import dedupliziert vor dem Persistieren nach `uopid`
- Fuer den aktuellen Fahrplanbuilder wurden vier synthetische Grenzverbinder mit `0m` Laenge eingefuegt, damit Basel Bad Bf, Schaffhausen, Konstanz und Kreuzlingen im CH/DE-Graph routbar bleiben

## Offene TODOs

- [ ] Customer Views
- [ ] Business-/Geschaeft-Views
- [ ] Fahrplanversionierung statt 1:1-Archivbeziehung
- [ ] TTT-Export / Versand fuer `tttRelevant` Fahrplanzeilen
- [ ] Lazy Loading / Pagination bei grossen Datenmengen
- [ ] Erweiterte E2E-Tests fuer Fehlerszenarien und Imports

## Bekannte Issues

- Routing ist bewusst datengetrieben: wenn im geladenen OP-/SoL-Graph ein Segment fehlt, blockiert der Fahrplanbuilder mit `No route found`
- Repo-weit existieren noch Formatabweichungen fuer `spotless:check`, die nicht funktional am aktuellen Feature haengen

## Security Backlog (offen, dokumentierte Design-Entscheidungen)

Aus dem Codex-Pen-Test 2026-05-05 stehen offen:

- **H2 (HIGH) Reverse-Link / Cross-Domain Sichtbarkeit** — `BusinessRepository.findByLinkedOrderId` / `findByLinkedOrderPositionId` und die Reverse-Link-Sektionen in `OrderDetailView` / `OrderPositionDetailView` filtern nicht nach User-Scope. Konsistent mit dem etablierten Modell „alle authentifizierten Benutzer sehen alles" — wenn Multi-Tenancy/Owner-basierte Sichtbarkeit gewuenscht wird, muss das fuer Aufträge, Geschaefte UND ihre Reverse-Links gleichzeitig eingefuehrt werden, separater Tag Aufwand.
- **M2 (MEDIUM) User-Enumeration ueber AssigneeComboBox** — jeder authentifizierte UI-Benutzer kann mit `/businesses` Zugriff Keycloak-User und -Gruppen ueber `KeycloakUserService.searchUsers/searchGroups` enumerieren. Mitigation-Optionen: rolle-basiertes Gating der Combo (nur ADMIN/DISPATCHER), serverseitige Rate-Limits, oder Wechsel auf einen abgeleiteten Username-Cache. Verschoben, weil das Daten-Modell „Bearbeiter aus Keycloak waehlen" gerade erst eingefuehrt wurde.

## Architektur-Entscheidungen (ADRs)

1. **ADR-001**: Spring Security OAuth2 statt Keycloak Adapter
2. **ADR-002**: Flyway statt Liquibase
3. **ADR-003**: Hibernate Envers fuer Audit Trail
4. **ADR-004**: Accordion + Status-Chips als Primaransicht fuer Auftraege
5. **ADR-005**: Kompakter Order-Header + Dialoge/Fullscreen statt ueberladener Einzelseite
6. **ADR-006**: Bestellkalender mit TTR-Phasen
7. **ADR-007**: ERA RINF als Infrastruktur-Stammdaten
8. **ADR-008**: CSV-Import fuer Schlagwort-Katalog statt SQL-Seed
9. **ADR-009**: `FAHRPLAN`-Positionen speichern den Detailfahrplan nicht selbst, sondern verlinken 1:1 auf `timetable_archives` ueber einen `CAPACITY`-Ressourcenbedarf
10. **ADR-010**: Shift/Stretch-Zeitpropagation mit Pin-Konzept fuer Fahrplan-Zeitbearbeitung
11. **ADR-011**: Path Manager Architektur — REST API als Integrations-Boundary, Shared DB mit pm_-Prefix, statische State Machine
12. **ADR-012**: Vehicle Planning mit Gantt Chart — historisch, die aktive Funktion wurde am 2026-05-22 entfernt
13. **ADR-013**: TTR Phase Automation — TtrPhaseResolver berechnet Phase aus Datum + Fahrplanjahrstart, Auto-ProcessType, phasenabhaengige State Machine
14. **ADR-014**: Bloomberg-Style Master-Detail Layout, Keyboard-First UX, A11y Baseline — vereinheitlichtes `MasterDetailLayout<T>` fuer `/orders` und `/businesses`, neue Routes inkl. `/orders/{orderId}/positions/{posId}`, globale Vim-Style-Shortcuts, SkipLinks/BreadcrumbBar/AriaLive, Status-Pills mit Icon+Text statt Farbe-only, generische `user_view_preferences`

## Changelog

| Datum | Aenderung |
|---|---|
| 2026-05-22 | Fahrzeugumlauf aus aktiver Anwendung entfernt: `domain/vehicleplanning`, `/vehicleplanning`, UI-Komponenten, Navigation, i18n, Wiki/Konzeptdocs und direkte E2E-Szenarien geloescht. Historische `vp_*`-Migrationen bleiben fuer Flyway-Kompatibilitaet erhalten. |
| 2026-05-05 | Doku-Konventionen: `package-info.java` fuer alle aktiven Bounded Contexts + ui/infrastructure Top-Pakete (Aggregate-Roots, Beziehungen via @link, Authorization-Modell, Architektur-Constraints). `JavaDocCoverageTest` (JUnit) erzwingt Klassen-JavaDoc auf @Service/@Entity/@Repository/@RestController/@Component/@Configuration/@SpringComponent — bricht Build bei Verstoss. RollingStockRepository und RollingStockService nachdokumentiert. Methoden-Boilerplate bleibt verboten (CLAUDE.md). |
| 2026-05-04 | Master-Detail-Refactor `/orders` und `/businesses` mit Keyboard-First UX (vim-style `g o`/`g b`/`g h`, `Ctrl+K` CommandPalette, `Shift+?` Help-Overlay, `↑/↓/Home/End/Enter/Esc/n//`), A11y-Baseline (SkipLinks, BreadcrumbBar, AriaLive, Status-Pills mit Icon+Text statt Farbe-only). Generische `MasterDetailLayout<T>` (URL-driven Selection, role=listbox, type-ahead Filter). Neue Routes: `/orders/{id}/positions/{posId}` mit Cross-Order-Validation. `OrderDetailView` als `@SpringComponent`-Prototype mit `setMode(...)`. V29 (`business_purchase_positions` M2M + Audit), V30 (`user_view_preferences` generisch). `GridPreferenceBinder<T>` + `GridSettingsButton` fuer persistente Spalten-Reihenfolge/-breite/-sichtbarkeit. `BusinessLinksTree` (vereinheitlichter Auftrag->AP->BP-TreeGrid mit Checkbox-Spalte). LazyInit-Fix in `BusinessService.getLinked*Positions`/`getDocuments` (detached ArrayLists). Draft-Mode fuer neues Geschaeft (Dokumente + Verknuepfungen werden vor `create(...)` in-memory gepuffert). Resource->Bedarf Umbenennung (i18n only, DE/EN/FR/IT). `BusinessServiceLinkPositionsTest` (3 Faelle). ADR-014. Codex-Review-Befund OrderPositionDetailView Cross-Order gefixt; tote Imports/Felder entfernt. |
| 2026-03-31 | Dokumentations-Update Ressourcen/Bestellungen: datenmodel.md (neues Kapitel "Ressourcenbedarfe und Bestellpositionen" mit ER-Diagramm, Feldtabellen fuer ResourceCatalogItem/ResourceNeed/PurchasePosition, Enums, PurchaseStatus-Fluss, TTT-Integration Sequenzdiagramm, TttOrderAttributes JSONB-Struktur, V16/V17 Migrationen), ARCHITECTURE.md (ResourceNeedService, PurchaseOrderService, ResourceCatalogImportService, ResourcePanel, ResourceDialog, PurchaseDialog, TttOrderDialog, CatalogTab), BEDIENUNGSANLEITUNG.md (Kapitel 14: Ressourcen und Bestellungen mit 7 Unterkapiteln), PROJECT_STATUS.md (V15-V17 Migrationen, neue Komponenten, Changelog), GitHub Wiki (Resources-and-Purchases.md, Home/Sidebar/Bedienungsanleitung aktualisiert) |
| 2026-03-31 | Dokumentations-Update: datenmodel.md (VP ER-Diagramm + Feldtabellen, TTR-Phasen-Timeline + Phasentabelle + TtrPhaseResolver-Doku), ARCHITECTURE.md (VP Bounded Context + Komponentendiagramm, TtrPhaseResolver-Abschnitt), BEDIENUNGSANLEITUNG.md (Kapitel 13 Fahrzeugplanung, TTR-Phasen in Kap. 7), path-manager.md (TTR-Phasen-Abschnitt mit Badge-Farben + Bestellphase 2/3), vehicle-planning.md (neue Wiki-Seite), GLOSSARY.md (+12 Begriffe: VP, Gantt, TTR, Bestellphasen, CNA, Wendezeit, Mehrfachtraktion), ADR-012 (VP Gantt), ADR-013 (TTR Phase Automation), PROJECT_STATUS.md (Migrations V12-V14, Tests, ADRs) |
| 2026-03-31 | S-Bahn Olten-Aarau Full Integration E2E Test: 14 Tests in `sbahn-integration.spec.ts` (Order-Erstellung, Route, Hin-/Rueckroute, Halte, Zeitfenster, Auto-Zeiten, Soft-Delete/Undo, Speichern, Taktfahrplan 38 Positionen, PM-Senden, PM-TreeGrid, Archivansicht, Vehicle Planning, Cleanup). Screenshots in test-results/sbahn-*.png |
| 2026-03-31 | V12/V13-Migration: pm_journey_locations.country_code_iso VARCHAR(2)->VARCHAR(5) fuer dreistellige ISO-Codes (CHE, DEU). V12 fixte Haupttabelle + Audit, V13 Nachtrags-Fix fuer Audit-Tabelle |
| 2026-03-31 | Fahrzeugumlaufplanung (Vehicle Planning): Bounded Context `vehicleplanning`, V11-Migration (4 vp_* Tabellen), 4 Entities + 2 Enums + Conflict Record, 4 Repos, 2 Services (Planning + Conflict Detection), 3 UI-Komponenten (GanttChart, TrainPalette, ConflictPanel), VehiclePlanningView, MainLayout Nav + Breadcrumb, i18n DE/EN/IT/FR |
| 2026-03-31 | Timetable Builder 6 UX-Fixes: Eindeutige Zeitfenster-Labels (4 i18n-Keys), Grid 14->8 Spalten, PM Qualifier-Labels dynamisch, Route-Zeiten optional + Laufweg-Vorschau, Kompakter Kalender (setCompact), Gueltigkeit im Builder-Header |
| 2026-04-02 | Security Pentest: Separate API SecurityFilterChain, @PreAuthorize auf OrderService, Credential-Sanitisierung, Error-Handler Sanitisierung, SecurityAuditorAware, simulatedBy in ProcessEngine, GH Actions SHA-Pinning, SSL-Hinweis, Dev-Logging reduziert |
| 2026-04-02 | Code Review Backlog: 6 Findings (i18n Fallback sync, Route Graph Caching TTL 5min, Google Fonts CDN entfernt/GDPR, Leaflet map.destroy(), StatusBadge Dead Code, OrderAccordionRow extrahiert aus OrderListView) |
| 2026-04-02 | Codex Code Review: 14 Findings behoben (SecurityConfig, IDOR, State Machine MODIFIED, null-overwrites, LazyInit DiffController, JSON Builder, Stretch-Math, N+1, Ownership-Check, Validation, DiffService Composite Key, ddl-auto validate, JourneyLocation save, Halt-Validierung) |
| 2026-03-31 | Path Manager E2E-Test (8 Tests), REST API Bugfix (LazyInitializationException via @Transactional + @EntityGraph), Wiki-Screenshots aktualisiert (PM + Timetable), finale Dokumentation |
| 2026-03-31 | Timetable Builder UX: Route ohne Step-Wechsel (Erfolgs-Notification + gruener Button), interaktive Karte mit OP-Markern (Klick fuellt Formularfelder), kompakter Kalender als Details-Akkordeon in Step 2. i18n DE/EN/IT/FR. E2E-Tests angepasst |
| 2026-03-31 | Path Manager Doku erweitert: datenmodel.md (ER-Diagramm, Feldtabellen, State Machine, Integration), ARCHITECTURE.md (Komponenten, API), wiki/path-manager.md (User Guide), ADR-011, GLOSSARY.md (+16 Begriffe) |
| 2026-03-31 | Path Manager Integration: Send-to-PM Button in OrderPositionRow, OrderPositionPanel Handler mit PathManagerService/TimetableArchiveService, OrderDetailView DI. i18n DE/EN/IT/FR. Doku: datenmodel.md, timetable-builder.md, ARCHITECTURE.md |
| 2026-03-31 | Path Manager Vaadin UI: PathManagerView (TreeGrid + SplitLayout), TrainHeaderPanel, ProcessSimulationPanel, JourneyLocationPanel, TreeNode sealed interface. Navigation + Breadcrumb. i18n DE/EN/IT/FR |
| 2026-03-31 | Path Manager REST API: 9 DTOs, PathManagerDtoMapper, 3 Controller (PathManagerController 7 Endpoints, PathProcessController 3 Endpoints, PathManagerDiffController 1 Endpoint). Springdoc annotiert |
| 2026-03-31 | Path Manager Domain Layer: V9-Migration (8 Tabellen + Audit), 8 Entities, 4 Enums, 8 Repositories, 4 Services (State Machine, CRUD, Diff, Identifier), FK auf order_positions |
| 2026-03-31 | E2E-Test und Dokumentation fuer Timetable Archive View: Lifecycle-Test Schritt 3b, Datenmodell-Route, Wiki-Abschnitt, ARCHITECTURE.md Komponentenhierarchie |
| 2026-03-31 | Read-only Timetable Archive View: SplitLayout 65/35, Div-basierte Fahrplantabelle mit Farbcodierung, Karte, Gueltigkeit, Metadaten. View-Button in OrderPositionRow. i18n DE/EN/IT/FR |
| 2026-03-31 | Doku: OTN-Feld (Operational Train Number) in datenmodel.md, timetable-builder.md und PROJECT_STATUS.md dokumentiert. Freitext VARCHAR 20 auf `timetable_archives` und `order_positions`, V8-Migration |
| 2026-03-31 | Doku: datenmodel.md (neue TimetableRowData-Felder, Enums, Editing-Service), ARCHITECTURE.md (Komponentenhierarchie, Propagationsarchitektur), ADR-010 (Shift/Stretch), Wiki timetable-builder.md, GLOSSARY.md (6 neue Begriffe) |
| 2026-03-31 | Timetable-Editing Phase 2-4: Stop Add/Remove, Shift/Stretch-Propagation, Commercial-Modus, TimingQualifier-Tags, Pinned-Zeiten. TimetableTableStep in 3 Klassen aufgeteilt |
| 2026-03-31 | Dokumentation fuer `LEISTUNG`- und `FAHRPLAN`-Positionen, Builder, Archiv und aktuelle UI konsolidiert |
| 2026-03-31 | Fahrplanbuilder umgesetzt: 2 Schritte, OSM-Karte, kuerzester Weg ueber SoLs, TTT-nahe Zeitmodi, Archivspeicherung |
| 2026-03-31 | `timetable_archives` + CH/DE-Grenzverbinder per V7 eingefuehrt |
| 2026-03-31 | `LEISTUNG`-Dialog um OP-Auswahl, Pflicht-Zeitfelder und Kommentar erweitert |
| 2026-03-31 | Auftragsliste neu gestaltet: Summary-Metriken, Kommentarzeile, Status-Chips mit Zaehlern und Positionsfilter |
| 2026-03-31 | Schlagwort-Katalog aus SQL-Seed entfernt, CSV-Quelle `data/seeds/predefined-tags.csv`, Import im Settings-Bereich |
| 2026-03-31 | RINF-Import gehaertet: atomarer Replace-Import, DE Operational Points deduplizieren nach `uopid` |
| 2026-03-31 | Theme-Anwendung abgesichert und sofortiger Profil-Themewechsel repariert |
| 2026-03-31 | Playwright-E2E fuer Fahrplanbuilder ergaenzt |
